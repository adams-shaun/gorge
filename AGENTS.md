# AGENTS.md — gorge

Pure-Go Magic rules engine.

## What this is

The rules engine that will replace `mtgplay`'s XMage bridge. Card behaviour is
compiled from Forge card scripts into an IR; the engine implements the
primitives that IR references. See
`docs/superpowers/specs/2026-09-03-mtgcore-go-engine-design.md`.

## Hard rules

- **Never commit Forge card scripts.** They are GPL-3.0; gorge is Apache-2.0.
  `forgec fetch` pulls the card corpus and token scripts into `.cards/`,
  pinned to a commit SHA (`FORGE_REF` in the Makefile), which is gitignored.
  `cards/boundary_test.go` fails the build if any are tracked.
- **No cgo, no third-party deps** in the card pipeline and rules core.
  `wazero` arrives with the plugin tier in M3.
- **All state mutation goes through `events.Apply`.** If you are writing to a
  `state.Game` field outside `events`, you are introducing a replay bug.
  `events.Kind` is append-only, and every kind M2r and M2d added was appended
  after all earlier kinds so ordinals, the hash chain and golden replays stay
  untouched: M2r appended `CastInfo`, `Choose`, `TokenCreate`, `StackCopy`,
  `Attach` and `AbilityPush`; M2d appended `ModeChosen` for mid-resolution
  modal answers.
- **No nondeterminism.** No wall clock, no ambient randomness, no `map` range
  where iteration order can reach an event.
- **This engine never imports anything from the mtgbld/mtgserve application.**

## Build / run / test

```sh
make fetch-cards          # one-time; ~25 MB, pinned commit from Card-Forge/forge
make compile-cards        # parse into the IR cache
make report               # card coverage against implemented primitives
make sim                  # build mtgsim and play 20 verified 4-seat games
make test lint
make conformance          # the KNOWN-RED CR conformance lane, opt-in
```

`make conformance` is the one lane where **failure is the expected result**. It
runs the `GORGE_CR_CONFORMANCE=1` tests, which document Comprehensive Rules
behaviour this build gets wrong, so a red run is the status quo and a **PASS is a
signal to act**: the defect got fixed, and that leaf's `requireCR601Audit` guard
should come OFF so the ordinary suite defends it from then on. Fixing one leaf
does not green the lane -- check which leaf turned before closing any issue. The
rule is written out at `Makefile:145-149`; it is pointed at from here because it
governs what a reviewer must do at a merge and used to live only in the Makefile.

## Reproduce a feedback report

A player-submitted bug report that named a table carries a replayable
snapshot (`match.json`, `log.json`, the seat's `view.json`, captured by the
server's feedback capture). Turning it into a failing test is one command:

```sh
go run ./cmd/repro <feedback-dir>              # replay, verify the head, print the board
                                             # at the report point (turn, step, life,
                                             # battlefield, stack, last ~20 log lines)
                                             # (-at 0 also works flags-after-dir:
                                             #  `repro <dir> -at 0` is parsed same as
                                             #  `repro -at 0 <dir>`)
go run ./cmd/repro -at N <feedback-dir>        # replay to intent N instead
                                             # (-list prints the intent timeline to find N)
go run ./cmd/repro -omniscient <feedback-dir>  # show every hand in the summary
go run ./cmd/repro -emit-test <pkg> <feedback-dir>
                                             # copy the snapshot into
                                             # <pkg>/testdata/feedback/<id>/ and write a
                                             # failing test skeleton (feedback.EngineAt) as
                                             # the target package's EXTERNAL test package
                                             # (package <name>_test: feedback imports the
                                             # engine tier, so an internal-package skeleton
                                             # would be an import cycle for an engine-side
                                             # target); replace the TODO with the assertion

A committed fixture's `match.json` does not embed token script text — Forge
scripts are GPL-3.0 and must never be committed — so `-emit-test` and the
fixture generator strip `match.json`'s `tokens`/`tokens_unread` and sync the
exact text into the gitignored `cmd/repro/testdata/.tokens/<id>/` keyed by
the fixture id. `feedback.Load` resolves token scripts from that sync
directory first (exact historical text) and falls back to the live corpus
(`.cards/` at the current `FORGE_REF`) when it is missing (a fresh clone), so
a fixture still replays without ever re-embedding the text into committed
files.
```

Exit 0 is a verified replay: the rebuilt event stream matches the recording
event for event and the final head equals log.json's `head`. A non-zero exit
prints `DIVERGED` naming the first mismatching event — **a corpus change since
the report was filed is a real, expected cause** (the snapshot replays against
the corpus as it was at the report's pin), and so is an engine change; read the
first diverging event before assuming either. In a test, load the same
snapshot with `feedback.EngineAt(t, dir, n)` (`internal/testutil/feedback`;
`n < 0` = every recorded intent) and assert at the engine it returns.

The committed fixture `cmd/repro/testdata/feedback/20260914T120000Z-fb01/` is
a real capture produced by `host.SnapshotForFeedback`; regenerate it with
`REPRO_REGEN_FIXTURE=1 go test ./cmd/repro -run TestGenerateCommittedFixture`
after a corpus pin bump (`FORGE_REF`), or the fixture's own gate reports
DIVERGED exactly as a stale report would.

## Status

**M2r closed the coverage ratchet.** `rules/acceptance_test.go`'s
`knownUnsupported` (Ruling P12/D2-a) names exactly the cards the measured
deck set does not fully support, and
`TestEveryRepoDeckIsFullySupported` asserts the measured gap set equals the
table in both directions (Ruling R-20): a card the build newly cannot fully
support fails and is named together with the primitives it is missing, and a
table entry the build now supports is stale and fails too. Measured
2026-09-17 at the avengers-assemble Commander import, the ratchet stands at
**3 of 791** -- every one of the 791 distinct cards across the repo deck
files (`internal/testutil/decks/*.json`: 14 60-card constructed decks plus
10 Commander decks) is fully supported and plays except the three the table
holds (Avengers Quinjet's crew, Captain Marvel's mirror-counter trigger,
Speed's pay-on-trigger -- the Marvel Super Heroes Commander import's own
remaining backlog). The 12 pinned Legacy decks (`legacyDeckNames`)
round-robin across 2/4/6/8 seats
(`TestRepoDecksPlayAtEverySeatCount`), replay byte-identically
(`TestRepoDeckGamesReplayExactly`), and `TestHeads` pins the chain heads as
goldens in `rules/heads_test.go`:

| seats | 2 | 4 | 6 | 8 |
|---|---|---|---|---|
| chain head | `6085b337c4a4c0a1` | `22cf2bf04aeab3c7` | `100db196f20a293a` | `35522b0d8c5d2bf9` |

`TestEveryRepoDeckParamsAreRead` (`rules/paramcensus_test.go`) is the
companion ratchet over the same decks' parameters: measured at the same
date, its `knownUnsupportedParams` table holds 25 repo-deck cards carrying
an unread param or unmodelled cost token (19 of them from the
avengers-assemble import; the desc-only `ValidTgtsDesc` family the new
deck's cards carry several of).

`make sim` plays 20 verified 4-seat games from the same seed set, every one
replaying byte-identically (20/20 `replay OK`).

Measured at the corpus pin `master @
95f04e8a04c8925fa97cb226fc3341cabcc90a53` (`FORGE_REF` in the Makefile):
`make report` prints `cards: 33667  playable: 24727 (73.4%)` with `tokens:
839` (re-measured 2026-09-17; the jump from the 2026-09-14 figure of
21108/62.7% is the parameter-read registrations merged since -- the
param-census task wave -- and the pw1 figure 20635/61.3% predated api:Untap,
api:ManaReflected, stat:ManaConvert, stat:UntapOtherPlayer and
kw:Cumulative upkeep registering. `repl:Untap` is out of scope and remains
unsupported. The registered primitive set is measured by `make report`.
M1's 37/8/8/8/1 = 62 was the count before M2r registered the keyword,
trigger, static and mid-game primitives the ratchet's card work needed.)

A seat's clients must be able to answer every `decision.Kind`, and the set is
closed: the M1 kinds (`priority`, `target`, `attackers`, `blockers`,
`trigger_order`, `trigger_optional`), M2r's `choose` (an {X} value, delve
exiles, cost sacrifices, "as it enters" name/type/number, miracle-style
yes/no), the two M2d closures -- `mulligan`, the London keep/mulligan
and bottoming round `Config.Mulligans` runs between the deal and turn 1, and
`modes`, the modal pick and unless-pay ask a mid-resolution answer serves
(the `ModeChosen` event carries the answer into the log) -- and the three
kinds the later card work added: `commander_zone` (a commander's OWNER's
CR 903.9 command-zone replacement choice), `replacement` (the CR 616.1
order choice over competing replacement effects) and `arrange` (the ordered
subset ask `Scry`/`Surveil`/`RearrangeTopOfLibrary` share, Ruling J0).
Concede (M2d-3) is not a kind: it is a `concede` option on every priority
decision that emits the existing `PlayerLost` with Text "conceded". The
engine-side defaults that still stand in for a choice the engine cannot yet
ask are listed under **Known approximations** below.

Acceptance commands:

```sh
go test ./rules/ -run 'TestEveryRepoDeck|TestRepoDecks|TestRepoDeckGames' -v
make sim
```

## Known approximations

Where a registered primitive is implemented with an engine-side default for
a player choice the engine cannot yet ask, or with behaviour narrower than
the card text, it is listed here with the stand-in's location and the
milestone that removes it. Each entry was verified live at `main` `6efda17`;
a stand-in whose code no longer exists must be deleted from this list, never
kept -- a deferral that names a milestone is only as good as the milestone
still owing it.

| Stand-in | Where | Removed by |
|---|---|---|
| `Affected$ You | AddKeyword$ Shroud` — PLAYER shroud (True Believer, Ivory Mask; 4 corpus files carry the exact grant shape, 3 carry those two cards) — is unread: players have no ObjID and no keyword machinery at all (the layer walk never reaches `Affected$ You` for keywords), so a player with granted shroud stays targetable by every spell and ability. PERMANENT shroud is real (CR 702.14 enforced in `candidatesFor`/`legalTargets` via `shroudBlocksTarget`), this row is only the player half. | `rules/stack.go` (`candidatesFor`, `legalTargets`) | M4 (player keyword machinery — a player-shaped Derived set the layer walk can fill) |
| `AB$ ManaReflected` asks the colour whenever the reflected set holds several (the ordinary chooseManaColor ask, candidates in fixed WUBRG order), adds a SINGLE candidate without an ask, and -- reached standalone, outside the mana-activation path -- falls back to the deterministic first candidate (the R-9 no-ask contract). ColorOrType$ Type includes {C} only when a reflected mana ability can produce it; ReflectProperty$ Produce reads every mana ability's producible symbols ("Any"/"Combo Any" = all five colours, "Combo R G" = exactly those); ReflectProperty$ Is reads effects.ColorsOf; ReflectProperty$ Produced reads the fixed-order mana types captured from its causing mana event and gives the extra mana to its Defined$ player. `trig:TapsForMana` IS registered (`rules/trigger_match.go`'s `RegisterNonAPI` list, with event-role capture in `rules/trigger_referents.go`), so the Produced$ reflection above is exact. `Imprint$ True` persists Chrome Mox's player-selected exile (including the resumed multi-card choice), and `Defined.Imprinted` reads an ID only while that card remains in exile; Valid$ specs with unknown predicates fail closed to an empty set; `RestrictValid$`/`Amount$` and the `IsPresent$`/`PresentCompare$` activation gate ARE read (`effects/mana_reflected.go`, `rules/mana_activation.go` `manaReflectedPresentHolds`, enforced via `rules/stack.go` `manaAvailableFor`/`restrictValidMatches`). The ManaProduction collector (cards/mana_production.go) and colour identity deliberately do NOT see reflected mana -- the membership was added at the one call site the rules need (availableManaAbilities), so a bot never taps a reflected source for its coloured pips. | `effects/mana_reflected.go` (`effManaReflected`, `ManaReflectedCandidates`), `rules/mana_activation.go` (`availableManaAbilities`, `resolveManaEffect`) | M4 (tap-to-pay for reflected sources; a bot-quality reflected-source collector) |
| `stat:ManaConvert` is wired for the battlefield S: statics only: the Effect-granted family (`SVar:...Mode$ ManaConvert`, 22 raw occurrences, e.g. SpendAnyMana) and the EffectZone$ Command statics never reach the payment path; `Optional$ True` (Jodah's one-shot) is unread; a ValidCard$ this build cannot evaluate (Card.NamedCard, cmcEQChosen, Case.YouCtrl, Card.YouDontOwn+YouCtrl) fails closed and never grants. Grammar implemented: AnyType->AnyColor (wild over coloured pips), AnyType->AnyType (wild + the {C} pips), <Colour>-><Colour> (White->Red), and the compound `White->AnyColor nonWhite<-C` restriction (non-white mana may pay ONLY {C} pips and generic, not even its own colour's pip). The conversion applies to every cast/ability/mana-ability payment (costPayable and payManaConv share one resolveMana), but NOT to the mid-resolution unless-pay arm, which pays via plain payMana. Pool-only like every payment here: no tap-to-pay, so a pip is payable only from floating mana the conversion reaches. | `rules/mana_convert.go`, `rules/mana.go` (`resolveMana`'s conv), `rules/stack.go` (`payManaConv`, `costPayable`, `paymentConv`) | M4 (Effect-granted statics, unless-pay conversion, tap-to-pay) |
| `api:FlipCoin` (effFlipCoin) and `trig:FlippedCoin` are REAL since flipcoin1, with measured remainders. The flip itself is exact: one seeded `h.Rand(2)` draw per flip, recorded on the canonical result Note (`effects.FlipCoinNote` — `Player` = flipper, `Amount` 1 = heads/win, 0 = tails/lose, Text `"flips a coin: heads"/"tails"`), the ONE encoding `trig:FlippedCoin`'s matcher reads and the cumulative-upkeep `FlipCoin<1>` cost action (`rules/cumulative.go`) now shares, so Karplusan Minotaur's cost-side flip fires its triggers exactly like an effect-side one. Read: `WinSubAbility$`/`LoseSubAbility$` plus the `NoCall$` spellings `HeadsSubAbility$`/`TailsSubAbility$`, `Amount$` (that many independent flips, each running its own branch), `Defined$`/`Flipper$` through one shared `knownDefinedTargets` resolver (Remembered, TriggeredActivator, Player selectors), `FlipUntilYouLose$ True` (5 raw lines: flip until the first tails — narrowing, corpus-unreachable: a win branch that suspends abandons the loop rather than resuming it, since the resume re-enters the branch SA not the primitive). **Unread, each loud when present**: `ForEachPlayer$` (1 raw line — one flip instead of one per opponent); the per-flip memory `RememberResult$` (2) / `RememberNumber$` (2) / `RememberLoser$` (1 — Unleash the Flux's Repeat gates on the loser being you, so with the memory dropped that loop never repeats); and `Defined$ FlippedHeads`/`FlippedTails` resolve to the EMPTY set (fail closed, `effects/context.go`'s definedSpec), so a later sub reading the unrecorded result acts on nobody rather than falling back to the flipping source. The `NoCall$`/`LockInText$`/`…Desc$` family is cosmetic. `ValidResult$` Win/Lose and `ValidPlayer$` are exact (8 trigger lines). | `effects/flipcoin.go` (`effFlipCoin`, `FlipCoinNote`, `FlipNoteResult`, `flipperPlayers`, `rememberFlags`), `rules/trigger_match.go` (`flippedCoinMatches`), `rules/cumulative.go`, `effects/context.go` (`definedSpec`) | M4 (the flip-memory parameters, `ForEachPlayer$`, and the until-you-lose suspension resume) |
| `K:Cumulative upkeep` expands to a real Phase trigger and therefore uses normal trigger ordering, responses and stack resolution; only resolution adds the age counter and opens CR 702.46b's mana-ability payment window. Mana, hybrid, Phyrexian and `PayLife<N>` costs use the ordinary conversion-aware payment path. All **12 raw printed action-cost lines** execute and scale per age counter: `Sac` x2 plus Draw, Discard, AddMana, ExileFromTop, FlipCoin, GainLife, GainControl, PutCardToLibFromSameGrave, AddCounter P1P1 and AddCounter M1M1; actions that identify an object/player/graveyard pose a real choice. Snow {S} still degrades to one generic, and dynamically granted cumulative upkeep (`KW$ Cumulative upkeep:...`) remains outside printed-keyword expansion. | `cards/keywords.go`, `rules/cumulative.go`, `rules/mana.go` | M4 (snow costs and dynamically granted keywords) |
| Target decisions expose only the active SA API and nominal literal damage (`target_effect`); missing/dynamic/X/SVar/invalid amounts are null, not zero. This is not a lethal forecast: prevention, replacement, conditions, divided damage, target legality at resolution, and parent/sub-ability effects are not modelled by this summary. `ChangeZone` alone does not identify hostile removal; unknown APIs must stay uninterpreted. The bot policy consumes it only for a known literal damage amount (see the bot targeting row below); every other shape stays uninterpreted. | `decision/decision.go` (`TargetEffect`, `DamageEffect`), `rules/stack.go` (`describeTargetEffect`) | richer outcome modelling |
| "As this enters, choose ..." is asked at cast/play time, so the choice is recorded and visible a resolution early; the mid-resolution machinery that could move it to resolution time exists (M2d-2) but the asks have not migrated | `rules/cast.go` (`etbAsk` -- the ask at ~750-762; `etbAnswer` -- the emit at ~919-934) | M4 |
| `Mana` with `Produced$ Any`/`Combo Any` adds colourless instead of asking (Cavern of Souls, Chromatic Star, Lion's Eye Diamond, Lotus Petal). **Special-selector coverage** (measured): `Special LastNotedType` (Jeweled Amulet, Ice Cauldron) resolves, and `Special EachColorAmong_Valid <spec>` (Faeburrow Elder, Tarnation Vista's `+MonoColor` variant — which added the `MonoColor` predicate to `effects/filter.go`, the twin of `MultiColor`) resolves as a deterministic WUBRG-ordered batch over the battlefield scan `ManaReflectedCandidates` uses, empty colour set = silent no-op; the remaining Special selectors stay loud-fail — `EnchantedManaCost` 2 files (bloom_tender, elemental_resonance), `DoubleManaInPool` 1 (doubling_cube), `EachColoredManaSymbol_Milled` 1 (charmed_pendant), `EachColorAmong_ExiledWith` 1 (sunbird_standard_sunbird_effigy) | `effects/misc.go` (`effMana`) | M4 |
| `RestrictValid$` spend restrictions are never read (Cavern of Souls' second mana ability, Eldrazi Temple's {C}{C}) -- colour and restriction both come out as plain colourless | `effects/misc.go` (`effMana`) | M4 (mana restrictions) |
| A copy always keeps the original's target: `MayChooseTarget$` IS read -- but only to gate a Note ("copy keeps its targets", `effects/copy.go` ~157-164), not to offer a chooser -- so the copy's target selection still stays a stand-in (Chain Lightning's pay-copy clause; the unless-pay ask itself is real since M2d-2). The `UnlessSwitched$` orientation IS now read (sw1b): paying causes the copy on the switched shapes (Chain Lightning) and stops it on the unswitched shape (Wandering Archaic). The missing target chooser, not the flag being unread, is the remaining stand-in | `effects/copy.go` (`effCopySpellAbility`) | M4 |
| `Discard` implements the two asking shapes plus a filter now. `Mode$ RevealYouChoose` (Thoughtseize, Duress) is a mid-resolution KModes ask: the CASTER chooses from the DiscardValid$-filtered hand, and the answered choice re-enters to discard exactly those cards (a chained `SubAbility$` such as Thoughtseize's `DBLoseLife` fires exactly once, once the choice is in). `Mode$ TgtChoose` (Mind Rot, Faithless Looting, Thirst for Knowledge, Riddlesmith) is the ordinary "discard N cards": the DISCARDING player — the target (`p`), not the caster — chooses from their own hand, without it being revealed first, via the same `ResumeKind: discard` arm and a chained `SubAbility$` also fires exactly once. `Mode$ RevealDiscardAll` (Cabal Therapy) is a filter, not a choice. `Mode$ Hand` (the wheel family: Reforge the Soul, Windfall, Magus of the Wheel, Dragon Mage, Dark Deal) is READ since the hand1 fix: the ENTIRE hand, one `events.Discard` per card in hand order, no ask and no Note (a mandatory line has no choice to record), the `RememberDiscarded$`/`RememberDiscardingPlayers$` riders applied per card (what Windfall's chained draw reads). Measured population at the hand1 fix: **108 raw lines / 104 files / 112 distinct card names**, none carrying `NumCards$`, `DiscardValid$` or `AnyNumber$` (0/0/0 — the arm reads none of them). The **5 raw `Optional$ True` Hand lines** (will_of_the_jeskai, ruin_grinder, raphaels_technique, snort, sail_into_the_west) keep the deterministic stand-in: the whole hand is discarded plus one Note ("may discard resolved as discard (no engine host to ask)") — a real may-discard election is M4. The reported feedback fb-20260918T072140Z-4ca8e286 (Reforge the Soul discarded ONE card per player; everyone kept old-hand + 7 drawn) is fixed and pinned end to end on the real corpus cards in `rules/reforge_the_soul_wheel_test.go`. **The rest still takes the front card deterministically** — `Random`, `Defined`, `LookYouChoose`, `YouChoose`, `RevealTgtChoose`. **No corpus Discard SA omits `Mode$` entirely** — over the 1099 raw lines `... | /usr/bin/grep -cvE 'Mode\$ '` is 0, and nothing in Go synthesises a `Discard` SA (`effDiscard` is only ever reached from compiled corpus SAs) — so the default arm's real population is those modes rather than any 'no `Mode$`' shape. Counts (compiled corpus `.cards/ir.gob.gz` at FORGE_REF, counted with a one-off Go walker: per face walk `Abilities`/`Triggers.Effect`/`Repls.With` plus every `SVars` body via `cards.ResolveSVar` — `Primitives`' traversal plus every `SVars` body — dedup each face by canonical SA signature): **1089 distinct engine-resolvable Discard SAs** = `TgtChoose` **797** (804 raw lines), `RevealYouChoose` **80**, `RevealDiscardAll` **13**, `Hand` **105** (108 raw lines), `Random` **58**, `Defined` **26**, `LookYouChoose` **7**, `YouChoose` **2**, `RevealTgtChoose` **1**. The raw corpus has **1099** Discard SA lines (`/usr/bin/grep -rhIE '\b(SP|DB|AB|ST|RE)\$ Discard\b'` = AB 133 + DB 802 + SP 163 + ST 1); 1099/804/108 are exact RAW-line counts and were not wrong, while 1089/797/105 are compiled SAs deduped per face — both populations are correct and count different things. **The default-`hand[0]` population is 94 on the deduped population** (1089 − 797 − 80 − 13 − 105), 94 on the raw lines (1099 − 804 − 80 − 13 − 108) — `Hand` moved out of it at the hand1 fix. `AnyNumber$` (22 `TgtChoose` raw lines, every one also `Optional$ True`) and the `RememberDiscarded$` (105) / `RememberDiscardingPlayers$` (1) riders are READ since r2: an `AnyNumber$` ask is a real choice the moment one eligible card exists (Min 0, Max the eligible count -- the strict-supersets gate does not apply to it), each answered discard is remembered at ctx level AND event-backed on the source (the persistent list `Count$RememberedSize` and `Card.IsRemembered` read later); Mind Maggots' ETB is the pinned corpus carrier (`rules/remembered_referents_test.go`). `Optional$` is read in the two ASKING modes since r2 (it lowers the ask's min to 0, so "you may discard" can be answered with none -- the 44 `TgtChoose` raw lines carrying it keep a real choice); in `Mode$ Hand` it is read since hand1 as the stand-in gate (the 5 raw lines take the whole hand plus one Note — a real election is M4); on the other deterministic no-ask modes it stays unread. `TgtChoose`'s ask fires only when there are STRICTLY more `DiscardValid$`-eligible cards than `NumCards$`; a hand with fewer or exactly `NumCards$` eligible discards what it owns and asks nothing, so a decision nobody could answer differently is never emitted (one eligible card and `NumCards$ 1` is not a decision). A no-ask host on `TgtChoose` degrades to the front-of-the-FILTERED-hand stand-in with a Note (R-9) — note this differs from `RevealYouChoose`'s front-of-raw-`hand[0]` fallback, deliberately. `NumCards$` is read; 173 of the 797 `TgtChoose` lines omit it (default 1). `UnlessType$` (all 15 raw Discard lines carrying it are `TgtChoose` -- the only mode Forge's DiscardEffect reads it in) is READ since the final-stragglers sweep: when a card of the named type is in the hand, a KChoose election ("discard one <type> instead" vs "discard normally", `discard_unless` resume arm) precedes the ordinary path, and the unless arm's own multi-candidate pick re-uses the ordinary "discard" arm (Min == Max == 1); with no type card in hand the election is never posed. The strict-supersets no-ask rule is bypassed by the election, which is the point: the unless alternative is a real choice even when the eligible count equals `NumCards$`. `TgtChoose` parameters still unread (count = lines carrying it): `ConditionPresent` 21, `ConditionCompare` 17, `ConditionCheckSVar` 12, `ConditionDefined` 11, `ConditionSVarCompare` 11, `UnlessCost` 10, `UnlessPayer` 10, `UnlessSwitched` 5, `AILogic` 11, `Mandatory` 3, `TargetMin`/`TargetMax`/`TargetUnique`/`TargetsAtRandom`, plus `SpellDescription` 204 / `StackDescription` 18 / `TgtPrompt` 11 / `Cost` / `Activation*` / `PowerUp` / `Exhaust` / `PlayerTurn` / `SorcerySpeed` / `Planeswalker` / `Ultimate` / `IsCurse` / `ModeCost` / `PrecostDesc` / `CostDesc` / `DiscardValidDesc`; `SubAbility` (240) is chained by `Resolve`, not read by `effDiscard` itself. Multi-target `TgtChoose` shares `RevealYouChoose`'s existing abandonment of targets 2..n (the first `h.Ask` suspends and re-entry sees `Ctx.Discard != nil` for every target): **127 of the 797** `TgtChoose` SAs are multi-target (multi-seat `Defined$` = `Player`/`Opponent`/`Player.Opponent`/`Player.Other`/`Player.IsRemembered`/`Player.NotedForDiscard` or `TargetMax$ > 1`). The CR 514.1 cleanup-step discard is NOT this primitive at all — it is a real `KChoose` ask in `rules/combat.go`'s `cleanupStep`, and `effDiscard` is not on that path | `effects/cardflow.go` (`effDiscard`) | M4 |
| The `Ctx` re-entry answer fields are now **scoped to the primitive that asked** (the obsolete claim that they were not, and that none of the three was confined to its own primitive, is deleted): each resume builds a FRESH `Ctx`, so an answer never carries between separate resumes, and WITHIN one walk each of the three consumers clears its own field after reading -- `effCharm` takes `c.Modes` into a local and sets `c.Modes = nil` before the mode loop (fx41), `effDiscard` captures `c.Discard` into a local and sets `c.Discard = nil` before the target loop (fx42); `UnlessPay`'s consumer is now the shared unless gate, which reads and clears it at the top of every `effects.Resolve` pass (fx42's scoping, moved into `effects.unlessProceed` when the gate absorbed every API's `UnlessCost$`). `Modes` was the one with a live reentrant consumer: `effCharm`'s re-entry branch handed the same `Ctx` to `Resolve` for every mode and to the Charm's own `SubAbility$`, so a NESTED Charm reached below it read `Modes != nil`, took the re-entry branch, and ran the OUTER Charm's mode names against its own `SVars` instead of posing its own ask -- or, when a name resolved back into a chain containing itself, re-resolved endlessly to a stack overflow. **Fixed by fx41** (the names are taken into a local and `Modes` cleared before the loop; nothing reads `Ctx.Modes` after `effCharm` returns). Measured corpus-unreachable when fixed: of **797 distinct compiled Charm SAs, ZERO** reach a second Charm by either carrier -- a chosen `Choices$` mode's sub-chain, or the outer Charm's own `SubAbility$` chain -- so the pin in `rules/nested_charm_modes_test.go` is a synthetic fixture and says so. **`UnlessPay` and `Discard` are now scoped by fx42** (all three fields are confined to the primitive that asked, closing the row's booked work): each resume builds a FRESH `Ctx`, so an answer never carries between separate resumes, and WITHIN one walk each consumer now clears its field after reading. `UnlessPay` is consumed by the shared unless gate (`effects.unlessProceed` reads and clears it at the top of every Resolve pass), so a nested unless consumer reached below a consuming SA in the same `SubAbility$` chain poses its own ask instead of inheriting the answer — `effCounter`/`effCopySpellAbility` no longer carry any unless code of their own. `Discard` is read only by `effDiscard`, which now captures `c.Discard` into a local and clears it before the target loop, so a nested `Discard` below it poses its own ask; capturing first keeps the load-bearing multi-target behaviour (every target sees the same list, targets 2..n abandon) intact. Measured corpus reachability at FORGE_REF: for `UnlessPay`, **ZERO** top-of-walk consumers (a `Counter`/`CopySpellAbility` with `UnlessCost$`) reach another unless-pay consumer in the same walk, so the pin is a synthetic fixture; for `Discard`, exactly **2** — Gruesome Discovery and Last Rites, whose `TgtChoose` discard `SubAbility$` reaches a `RevealYouChoose` discard (the fx42 pin uses Gruesome Discovery's real compiled SA). Both pins are in `effects/fx42_reentry_scope_test.go`. Separately still live: `effRepeat` and `effCharm`'s inner `Resolve` have no `Suspended()` guard and `effCharm` does not re-check `Suspended()` between modes, so a future asking sub there would ask N times — **measured corpus-unreachable**: **0** Repeat-with-asking-sub and **0** Charm-with-asking-mode in the reachable graph, though **4** cards have a `ReplaceWith$` whose `With` is itself a discard asker (Chains of Mephistopheles, Indominus Rex Alpha, Magus of the Chains, Mox Diamond) and asks once via `effects.Resolve`'s own `Suspended()` check rather than through an unguarded loop. **The outer-continuation DROP this row used to describe is fixed** -- fx34 made the resume a linked chain so every suspended continuation resumes, not just the innermost | `effects/misc.go` (`effCharm`, `effRepeat`, `effCounter`), `effects/copy.go` (`effCopySpellAbility`), `effects/cardflow.go` (`effDiscard`), `effects/registry.go` (`Ctx`, `Resolve`) | M4 (add the `Suspended()` guards) |
| `Sacrifice` asks its sacrificing player now (CR 701.21a): a player-targeted line poses a real KChoose over the `SacValid$`-eligible permanents — `Min=Max=Amount` for a mandatory batch (only when strictly more eligibles exist than `Amount$`, the effDiscard strict-supersets rule; a batch that takes everything eligible asks nothing), `0..Amount` for a plain `Optional$`, and `Optional$`+`StrictAmount$` takes a two-step ask (a KModes "sacrifice N?" election, then an exact-batch KChoose) because KChoose cannot express the disjoint set {0, Amount}. `Amount$` (literal, X, SVar) is honoured by `sacrificeAmount`; an amount resolving to <= 0 never asks (no Min==Max==0 prompt over eligible permanents). `RememberSacrificed$` records each sacrificed object's LKI into `Ctx.Sacrificed` (Sacrificed$CardPower/CardManaCost/Amount) and the card into `Remembered` (Braids, Scapeshift, Victimize's `Rise`); the answered batch re-enters through `ResumeKind "sacrifice"`/`Ctx.SacPicks`, scoped to the exact target that asked so a nested sacrifice poses its own ask (fx42); each emitted batch takes one departure snapshot (BatchDepartures). The Annihilator keyword expansion carries its own count in its `Annihilator$` marker. A line naming neither `Defined$` nor `ValidTgts$` but a non-self `SacValid$` defaults to its controller (66 corpus lines); the no-host stand-in (R-9) keeps the deterministic first-`Amount$`-eligible pick under a secret Note. Still unread: `ValidCard$` (exactly one reachable line, Expert-Level Safe's `DB$ Sacrifice` with `Defined$ You` and `ValidCard$ Card.Self`, whose controller hands over the first zone-order permanent rather than "this artifact"). The general `Condition*` SVar gates on a sub's chain ARE now enforced by the shared SVar-condition gate (`effects.CheckSVarHolds`, shared with the statics' CheckSVar$ and the ability-offer gate; a `ConditionCheckSVar$`/bare-`Condition$ Kicked` gate is evaluated when it is the SA's only condition, and an unmodelled count body fails OPEN — run anyway, the documented conditionMet convention), which subsumes the narrow remembered-sacrifice bridge the pre-merge build carried. `Permanent.sharesCardTypeWith RememberedCard` (Braids's `SacValid$`) is a real predicate (`effects/filter.go`), so Braids's opponents choose among their own artifacts/creatures/lands/planeswalkers and RepeatEach loop bodies see the loop's outer Remembered. Object-targeted sacrifice is unchanged (sacrificed as-is; no regeneration shield consulted — sacrifice is not destruction). The reported face of the pre-ask defect (fb-20260916T201317Z-841054c5, Planar Engineering's "Sacrifice two lands" picking and taking one) is pinned end to end on the real corpus card in `rules/planar_engineering_sacrifice_test.go`: the Min==Max==2 ask over every eligible land, the answered (non-zone-order-first) pair being the two sacrificed under the `sacrificed` marker, the `SubAbility$` library search still asking and entering its four picks tapped, plus the no-ask eligible==Amount and zero-eligible shapes on the same card) | `effects/zone.go` (`effSacrifice`, `sacrificeAmount`), `rules/resolution.go` (`"sacrifice"` and `"sacrifice_optional"` resume arms), `effects/filter.go` (`sharesCardTypeWith`) | M4 (`ValidCard$`; the general condition grammar) |
| `CARDNAME` in a sacrifice cost now means the **source object id**, never another same-name permanent (CR 201.5). Both `castable` and `sacAsk` pass that source into `MatchesSpecFrom`; source-less filters fail closed. A bare `Sac<1/CARDNAME>` or `Sac<1/NICKNAME>` whose sole candidate is that source is now recorded without a singleton sacrifice KChoose (measured at the current corpus pin: 1174 raw CARDNAME lines and 10 raw NICKNAME lines). Forge's third field (`Sac<1/CARDNAME/this artifact>`) is display text, already stripped by `ParseCost`, not a second filter. **This does not make every cost containing CARDNAME playable.** Measured at FORGE_REF: the **broad raw token population** (any `Word<...CARDNAME...>` anywhere on a line) is **1507 raw lines / 1457 card files**; the **raw `Cost$`/`UnlessCost$` substring restriction** is **1506 lines / 1456 files**, including `AlternateCost$` on Gavel of the Righteous. Restricting to EXACT parameter keys instead gives **1505 raw lines / 1455 files**. These are distinct from the original brief's coincidentally equal 1455-file count from the narrower `<[0-9]+/[^>]*CARDNAME` regex. Through `cards.OpenCorpus`, walking face abilities, trigger effects, replacement bodies and every resolved SVar (including Sub chains), deduplicating per face by Kind/API/Params: **1473 compiled SAs / 1426 cards** carry a broad token; **1472 compiled SAs / 1425 cards** carry one in Cost/UnlessCost (also the cost-suffix result). The relevant self-sacrifice subset is **1174 raw Cost/UnlessCost lines / 1134 files** versus **1172 compiled Cost/UnlessCost SAs / 1134 cards** with a parsed Sac spec exactly CARDNAME; limiting the compiled subset to activation/cast `Cost` (not `UnlessCost`) gives **1169 SAs / 1131 cards**. Of those, **1003 compiled top-level non-mana AB SAs / 974 cards** enter the ability loop's candidate population BEFORE its other legality/zone gates; these are not counts of fully fixed cards. Exile/Discard/Return and other unsupported cost verbs remain unsupported; mana abilities still use their separate incomplete payment path. In particular a fetchland's `PayLife<1>` now pays one life rather than generic mana. Source-aware sacrifice filters also now give the existing Self/Other predicates their actual source. | `effects/filter.go` (`MatchesObjectCtx`), `rules/cast.go` (`castable`, `sacAsk`); corpus regressions in `rules/cardname_cost_test.go` | M4 (remaining cost grammar and mana-ability payment) |
| `UnlessCost$`/`UnlessPayer$` are now ONE shared gate for every API: `effects.Resolve` calls `effects.unlessProceed` before each effect dispatch, so the pre-gate state — only `Counter` and `CopySpellAbility` read the parameter, every other API ignored it (Sacrifice 155 raw lines, Tap 56, Draw 43, LoseLife 42, ChangeZone 28) — is gone. The gate poses a KModes pay ask to each `UnlessPayer$` payer in turn (a decline moves on to the next payer); `effects.UnlessPayers` resolves every corpus selector form (`Targeted*`, `Triggered*`, `Remembered*`, `Enchanted*`, `Imprinted*`, `Replaced*`, `Player`, `Opponent`, `ChosenPlayer`, `TriggeredDefendingPlayer`, the attacking forms), and a NAMED selector whose binding is unavailable is treated as a DECLINE, never as a fallback to the resolving controller — only the unqualified default falls back to the controller. Payment happens in rules' `unless_pay` resume arm: mana via `payMana` (floating pool; no mid-resolution tap-to-pay), fixed `PayLife<N>`, choice-bearing `Sac<...>`/`Discard<...>` components through `beginUnlessPayment`, Sacrifice's `DamageYou<N>` offer paid as real damage from the offering permanent (Vexing Devil — `ParseCost`'s flat-{1} substitution no longer buys it), and Ward keeps its own CR 702.21a handler (`beginWardPayment`) outside the shared gate. `UnlessSwitched$` is read as the orientation (default unswitched: paying PREVENTS the body; `True`: paying CAUSES it — Chain Lightning's pay-to-copy and Wandering Archaic's pay-to-not-copy both follow the flag, not a hardcoded default), and the `SubAbility$` walk is gated on `UnlessResolveSubs$` (`Always`/`WhenPaid`/`WhenNotPaid`; 41 raw corpus lines were unread before). An unless-cost the strict parser cannot price — an unfolded `X` (the 21 `UnlessCost$ X` Counter cards; `XX` likewise, Thassa's Intervention), `PayEnergy<N>` (27 files), `Return<...>`, `LifeTotalHalfUp`, `DefinedCost_*`, dynamic SVars, `Y`/`Z` — is a HARD DECLINE (`ParseUnlessCost`, never `ParseCost`'s flat-{1} substitution): the ask is still posed and recorded by `ModeChosen`, but it cannot be answered "pay". Still open (M4): a value for X folded into the payment from the cast's own X-choice or an SVar, tap-to-pay from untapped sources, and the exotic unresolvable selectors' real bindings | `effects/unless.go` (`unlessProceed`, `unlessPayerTargets`, `poseUnlessAsk`, `unlessSubsRun`), `rules/resolution.go` (`unless_pay` arm, `ParseUnlessCost`, `payUnlessCost`, `beginUnlessPayment`, `payUnlessDamageCost`, `beginWardPayment`) | M4 (X-cost grammar, tap-to-pay, remaining selector bindings) |
| `TargetType$` stack-kind targeting is now READ: `legalTargetCandidates`'s stack branch admits ability objects (minted by `AbilityPush`/`TriggerPush`/`DelayedPush`) for a `TargetType$` naming an ability kind, split by `state.TriggerOf` -- the one classifier `view`'s StackView.Kind "trigger"/"ability" also uses, so the two cannot disagree: `Spell`/`Instant`/`Sorcery` = card objects, `Activated` = ability objects, `Triggered` = triggered ability objects (a DelayedPush mint counts as triggered, CR 603.7), `SpellAbility` = all three; a TargetType$ naming no stack kind -- including absent -- stays Spell-only (the old narrow default). Token qualifiers `YouCtrl`/`OppCtrl` are read (the copy family's `Activated.YouCtrl`, Kang Dynasty's `Spell.OppCtrl`), and the Instant/Sorcery card-type restriction is read both ways -- the bare bases `Instant`/`Sorcery` (Spider Sense's `Instant,Sorcery,Triggered`) and the Spell qualifiers `Spell.Instant`/`Spell.Sorcery` (Sister of Silence's `Spell.Instant,Spell.Sorcery,Activated,Triggered`) admit only instant/sorcery card objects, never a creature spell (pinned by `TestInstantSorceryCounterRestrictsCardObjects`, `TestInstantCounterNotCastableAgainstCreatureSpell`). A countered ability leaves the stack to the CR 608.2m exile parking, never the graveyard (CR 701.5a), and mana abilities are still structurally off the stack (CR 605.3a: the offer loop skips `AB$ Mana`). Measured: 40 raw `.cards/cardsfolder` files carry `Counter` + an ability `TargetType$` (the brief's 28 did not reproduce); the TargetType$ value census at the corpus pin is 503 `Spell`, 15 `Spell,Activated,Triggered`, 11 `Activated.YouCtrl,Triggered.YouCtrl`, 11 `Activated,Triggered`, 11 `Activated`, 8 `Spell.singleTarget`, 5 `SpellAbility.singleTarget`, 5 `Spell.numTargets`, 3 `Triggered.YouCtrl`, 3 `SpellAbility.numTargets`, 1 each `Spell.OppCtrl`, `Spell.Instant,Spell.Sorcery,Activated,Triggered`, `Spell.Colorless,Triggered`, `Instant.singleTarget,Sorcery.singleTarget`, `Instant,Sorcery,Triggered`, `Activated.singleTarget`, `Activated.YouDontCtrl,Triggered.YouDontCtrl`, `Activated,Triggered,Spell.nonCreature`, `Activated,Triggered,Spell.Legendary`, `Activated,Triggered,Instant,Sorcery`. **What remains:** the OTHER TargetType$ token qualifiers are unread, so those restrictions widen to the whole kind set -- `singleTarget`/`numTargets` target-count arithmetic (`Spell.singleTarget` x8, `SpellAbility.singleTarget` x5, `Spell.numTargets` x5, `SpellAbility.numTargets` x3, `Instant.singleTarget,Sorcery.singleTarget` x1, `Activated.singleTarget` x1; 10 distinct TargetType$ values, 26 files), `YouDontCtrl` (1), `Spell.nonCreature` (1), `Spell.Colorless` (1) and `Spell.Legendary` (1) -- and the split degrades to "activated" for an ability object whose source or face is gone (or flipped since the mint), so a `Triggered`-only counter (3 corpus files) misses those while the broad shapes still reach them | `rules/stack.go` (`legalTargetCandidates`, `stackObjKind`, `stackTargetKindTokens`), `effects/misc.go` (`effCounter`), `state/triggerof.go` | M4 (the remaining TargetType$ qualifiers) |
| `ValidTgts$ Spell` with no `TargetType$` and no `TgtZone$` still searches the battlefield: `targetZones` adds the stack only when `TargetType$` names a stack object. No corpus card has this exact shape (the one `ValidTgts$ Spell` line in the corpus also carries `TargetType$`), so this is a latent footgun, not a live deviation -- recorded here so a future script that omits `TargetType$` is not silently inert. | `rules/stack.go` (`targetZones`) | M4 |
| A spell on the stack is offered with `Option.Kind` `"permanent"`, the label inherited from the battlefield branch it was cloned from (`legalTargetCandidates`). Nothing in `botpolicy/`, `view/` or `seat/` reads that string, so it is cosmetic -- but it is wrong on the wire, and correcting it moves the option lists and therefore all four chain heads, so it is deferred to a change that is already regenerating them | `rules/stack.go` (`legalTargetCandidates`) | M4 |
| `Effect` (a continuous effect from `StaticAbilities$`/`Triggers$` for `Duration$`) now registers a **real** continuous effect for the two RESTRICTION statics the wave named, instead of a bare Note: `Mode$ CantTarget` (Vines of Vastwood) is consulted by `rules/stack.go`'s `askTarget` alongside `protectedFrom` with `Activator$` scoping, and `Mode$ CantRegenerate` (Incinerate) blocks the shield-consumption path in `effects.ReplaceDestruction` via `Host.RegenerationDisallowed`. The registry is `rules.Engine.continuous` (rebuilt by re-execution on replay), and expiry is event-driven through the existing `Active()`/`EndOfTurnCleanup` discipline. **Measured** (both populations named): RAW `.cards/cardsfolder` Effect-SA lines carrying an explicit `Duration$` (`/usr/bin/grep` over `$ Effect` lines) — `Permanent` 354, `UntilTheEndOfYourNextTurn` 105, `UntilHostLeavesPlayOrEOT` 73, `UntilYourNextTurn` 70, `UntilEndOfCombat` 31, `UntilTheEndOfYourNextUntap` 22, `AsLongAsControl` 17, `UntilYourNextEndStep` 15, `UntilUntaps` 15, `EndOfTurn` 11, `AsLongAsInPlay` 7, `UntilHostLeavesPlay` 6, `UntilYourNextUpkeep` 5, and seven single-occurrence values -- `UntilTargetedUntaps`, `UntilStateBasedActionChecked`, `UntilHostLeavesPlayOrEndOfCombat`, `UntilEndOfTurn`, `UntilEndOfCombatYourNextTurn`, `ThisTurnAndNextTurn`, `End` (738 lines, re-measured at the ce1 gate; the 13 named counts above are all correct). COMPILED SAs (dedup per face by canonical signature, walked over `cards.OpenCorpus` faces' Abilities/Triggers/SVars) — `Permanent` 341, `UntilTheEndOfYourNextTurn` 105, `UntilHostLeavesPlayOrEOT` 73, `UntilYourNextTurn` 70, `UntilEndOfCombat` 31, `UntilTheEndOfYourNextUntap` 22, `AsLongAsControl` 17, `UntilUntaps` 15, `UntilYourNextEndStep` 15, `EndOfTurn` 11, `AsLongAsInPlay` 7, `UntilHostLeavesPlay` 6, `UntilYourNextUpkeep` 5, six single values (725). **What works:** an `Effect` whose `StaticAbilities$` names `CantTarget` or `CantRegenerate`, expired `UntilEOT` when the source is an instant/sorcery (the Vines/Incinerate one-shot shape) or carries an explicit this-turn `Duration$`, else by source-leaves-battlefield. `ContinuousEffect.UntilTurn` gives a next-turn `Duration$` a real turn boundary — computed at registration from the live turn/active-player rotation, kept by `active()` while `e.G.Turn <= UntilTurn`, dropped by `EndOfTurnCleanup` at equality — but that path is **corpus-unreachable groundwork, exercised only by synthetic fixtures**, and an earlier version of this sentence overclaimed it as covering the 175 lines below. It is consulted only when the same `Effect`'s `StaticAbilities$` ALSO names `CantTarget`/`CantRegenerate`, and gate-measured, **0** of the 51 corpus files carrying `Mode$ CantTarget|CantRegenerate` carries `Duration$ UntilYourNextTurn` or `UntilTheEndOfYourNextTurn`. The 175 next-turn `Effect` lines (**105** `UntilTheEndOfYourNextTurn` + **70** `UntilYourNextTurn`) name `Mode$ Continuous` (~140), `MustAttack` (4), `CantAttack` (7), `CantBeCast` (4) and 16 `Triggers$` SVars — every one still a Note, so none of them gets a lifetime at all. `effects.IsNextTurnDuration` also collapses BOTH spellings onto end-of-next-turn: correct for `UntilTheEndOfYourNextTurn` (pinned at turn 3 by `TestEffectContinuousExpiresAtEndOfNextTurn`), but a full turn too long for `UntilYourNextTurn`, whose true expiry is the START of the controller's next turn (Kardur, Doomscourge's oracle text is the check). **What still does NOT:** a `Triggers$`-bearing effect (Palace Jailer's command-zone "exile until an opponent becomes the monarch" trigger needs monarch state and a command-zone trigger this build lacks), every other `StaticAbilities$` mode, and every remaining `Duration$`, which `effectUntilEOT` routes to one of two WRONG lifetimes rather than its true expiry (an earlier version of this row put them all in source-leaves, which its own list contradicts): on an instant/sorcery source — checked BEFORE the duration switch — EVERY duration except the two next-turn ones becomes UntilEOT; otherwise `UntilYourNextEndStep` **15** and `ThisTurnAndNextTurn` **1** are UntilEOT by name (a turn EARLY for the latter), while `UntilTheEndOfYourNextUntap` **22**, `UntilUntaps` **15**, `AsLongAsControl` **17** and `UntilYourNextUpkeep` **5** fall to source-leaves. A restriction whose `ValidCard$`/`ValidTarget$` is a compound `IsRemembered` spec (`Card.IsRemembered+Creature`) is rejected rather than silently over-applied — measured, **none** of the 18 `CantTarget`/`CantRegenerate` statics carrying `IsRemembered` is compound | `effects/misc.go` (`effEffect`, `parseStaticLine`, `effectRemembered`, `effectUntilEOT`, `IsNextTurnDuration`), `rules/layers.go` (`restrictionBlocksTarget`, `RegenerationDisallowed`, `restrictionApplies`, `nextTurnFor`, `AddContinuous`, `EndOfTurnCleanup`), `state/continuous.go` (`UntilTurn`), `effects/damage.go` (`RememberDamaged$`), `effects/regeneration.go` | M4 (Triggers$ effects and full Duration$ grammar) |
| `DelayedTrigger` only implements `Mode$ Phase` (382 of the 456 corpus files using `DelayedTrigger`, the "at the beginning of the next end step, return it" shape Flickerwisp uses): it registers into `state.Game.Delayed` via an `events.DelayedRegister` and, on entering the registered phase, fires through the ordinary trigger drain as a `DelayedPush` minted ability that resolves the `Execute$` sub-ability. The other modes (`ChangesZone`, `SpellCast`, `ChangesController`, `DamageDone`, `AttackersDeclared`) still record a Note only. A delayed trigger's `ValidPlayer$` gate and `RememberObjects$ Targeted` (vs `RememberedLKI`) capture are not modelled; `effChangeZone` honours `RememberChanged$ True` so the delayed trigger remembers the object it moved. `Phase$` values resolve through the ONE shared Forge phase-name parser (`state.ParsePhases`, PhaseType.smartValueOf/parseRange parity), and the registration is `state.EarliestAfter` -- the FIRST set member the game will still reach, which for a single-step value is exactly that step and for the multi-step shapes (`Main1,Main2` 6 cards, `Upkeep->` 5 cards) is the first listed phase still ahead, matching Forge's one-fire removal. A registration whose source is gone, SVar unresolvable or controller lost is skipped but never collected, so it is re-scanned at every matching `StepChange` for the rest of the game | `effects/misc.go` (`effDelayedTrigger`), `state/game.go` (`Delayed`), `events/apply.go` (`DelayedRegister`/`DelayedPush`), `rules/trigger_match.go` (`checkDelayedTriggers`) | the remaining modes plus `ValidPlayer$`/`Targeted` capture |
| The cast-offer target census (CR 601.2c: a spell with no legal target may not be announced) is deliberately NARROW. `castTargetsAvailable` withholds a cast only for the unconditional one-target shape -- an SA with `ValidTgts$` and NO `TargetMin$`/`TargetMax$`, `Choices$` or `Announce$`, and not `API$ Charm`. Every other shape is still OFFERED and still relies on the post-push `askTarget` backstop, which counters/fizzles it after announcement instead of never offering it. So a `TargetMin$ 1` spell with zero legal targets remains castable, which is the same CR 601.2c divergence the narrow case just closed -- it is narrowed, not removed. This is deliberate: dynamic bounds and modal or announced choices cannot be evaluated before those choices are made, and offering-then-fizzling is the conservative direction. **Measured** over raw `.cards/cardsfolder` lines with `/usr/bin/grep` 3.11: of **14100** raw `ValidTgts$` lines, **1901** carry at least one escaping parameter (`TargetMin$`/`TargetMax$` **1807**, `Choices$` **90**, `Announce$` **13**, `API$ Charm` **0** on `ValidTgts$` lines) and so keep the old behaviour. These are RAW LINE counts, an upper bound on affected script sites -- not a count of cards that misbehave in play, since most such spells have a legal target available anyway. | `rules/legal.go` (`castTargetsAvailable`), `rules/stack.go` (`legalTargetCandidates`) | M4 (target counts evaluated with announced choices in hand) |
| `ActivationLimit$` is now enforced for a computed limit. `activationLimitReached` (`rules/legal.go`) used to `strconv.Atoi` the value and return false (unenforced) for anything not a plain integer, so Withering Wisps' `ActivationLimit$ X` (`SVar:X:Count$Valid Swamp.Snow+YouCtrl`) was offered without limit: with ZERO snow Swamps its maximum activations is zero and the engine still offered the ability. `resolveActivationLimit` now routes a non-literal value through the Count$/SVar evaluator (`effects.EvalCount`, bound to the source object and its SVar table) so a computed limit is enforced; the offer-time read is a pure read (no event is emitted) and replay stays byte-identical for every game that does not involve such a card. Literals are unchanged (Basking Rootwalla's `1`). Making `Count$Valid Swamp.Snow+YouCtrl` count required adding `Snow` as a recognized predicate in `effects/filter.go` -- it had failed closed to zero (an unknown predicate never matches), so Withering Wisps' count was always 0; the `.Snow` predicate appears in 14 raw `Count$Valid` lines over 7 distinct specs. **Measured populations.** RAW `.cards/cardsfolder` lines (GNU `/usr/bin/grep`): `ActivationLimit$` on **296** lines -- literal `1` x283, `2` x7, `3` x3; non-literal `X` x1, `1:Activate only once each turn` x2, `1:Activate only once each turn:Flavor Chaosbringer` x1. COMPILED SAs (dedup per face by canonical signature, over `.cards/ir.gob.gz`): `1` x130, `2` x5, `3` x3, `X` x1 -- the only non-literal is `X`, and it now resolves. The three `K:Equip` keyword lines that carry the rider (`leather_armor`, `dark_knights_greatsword`, `shredders_armor` -- `K:Equip:<cost>:::ActivationLimit$ 1:...`) reach a compiled SA since the eqcm1 Equip-expansion rework: `cards/keywords.go` now passes the `ActivationLimit$` trailing field through verbatim, so the limit is enforced. **Remaining unresolvable:** an expression that is neither a literal, nor an SVar reference, nor an inline `Count$` stays unenforced (today's behaviour) because `resolveActivationLimit` reports ok=false -- measured on the compiled corpus that population is **0**. | `rules/legal.go` (`activationLimitReached`, `resolveActivationLimit`), `effects/filter.go` (`Snow` predicate), `effects/count.go` (`Num`, `EvalCount`) | none (the only unimplemented shape is a value that is neither a literal nor an SVar/`Count$` reference, and the compiled corpus carries none of them) |
| **`AllTargeted$` binds `Ctx.Targets` -- the resolving SA's own chosen targets -- rather than Forge's union over the whole root-ability/sub-ability chain (alltargeted1)**: registered in `refTargets` (`effects/count.go`), so `AllTargeted$Valid <spec>`/`CardManaCost`/`CardPower` resolve through the ordinary `<Ref>$<Property>` machinery and `/Op` applies. The engine defers a sub-ability's targeting to resolution, so a cost reduction that must count a SUB-ability target still reads 0 at every point the head is read. Measured corpus: **3** raw lines -- `raft_security_officer` (LIVE end to end: the ability's own `ReduceCost$ X` is re-evaluated target-aware at CR 601.2c in `repriceForTargets` via the new `pendingCast.ownReduce` net-adjust, pinned on the real card), `wayta_trainer_prodigy` (its `Count$Compare Y EQ2.2.0` needs the `DB$ Fight` sub-ability's second target PRE-asked at activation, Forge's `SpellAbility.setupTargets()` shape -- still dead), and `urgent_necropsy` (its `AllTargeted$CardManaCost` has no consumer at all because the `CollectEvidence<X>` cost token is unparsed by `ParseCost` -- still dead) | `effects/count.go` (`refTargets`), `rules/legal.go` (`ownReduceCost` target-aware), `rules/cast.go` (`repriceForTargets`, `pendingCast.ownReduce`) | M4 (sub-ability target pre-asking at activation, CR 601.2c; `CollectEvidence` cost parsing) |
| `Count$Compare` is implemented for the clean `<Name> <OP><threshold>.<ifTrue>.<ifFalse>` shape: `evalCountBody`'s `Compare` head (`effects/count.go`) resolves `<Name>` as the compared value (SVar body recursively through `EvalCount`, else the token itself as an inline expression), compares under `<OP>` ∈ {GE,GT,EQ,LE,LT} against a literal threshold, and returns the matching branch -- a literal or an SVar name resolved the same way -- under a depth cap (`maxCountDepth` 8, the first recursive path in this evaluator, so a self-referential SVar table terminates deterministically instead of overflowing). **Measured** (raw `.cards/cardsfolder`): **148** occurrences over **145 files**, **128** in `SVar:` bodies; **134** are the clean all-numeric-branch shape (GE 112, EQ 15, GT 5, LE 2) and **14** are exotic singletons -- of which **12** have a parseable threshold and resolve as far as their inner heads do (an unmodelled inner head degrading to 0 per convention: `GE4.X.4` my_wealth_will_bury_you, `LT5.X.Z` apocalypse_hydra, `LT6.X.Z` shatterskull_smashing, `GE1.Y.Z` cut_propulsion, `GE1.W.X` craters_claws, `EQ7.1.Y` the_biblioplex, `GE1.Z.4` starting_town, `GE20.20.Z`, `GE4.4.T` blood_lust, `GE4.4.T3` faerie_dragon, `EQ1.SacT.1` shilgengar, `LT5.AltCostPaid.Z` bringer_of_green_zeniths) and **2** have no parseable threshold and fail closed (`GEMePlus.3.2` teachings_of_the_archaics, `LTZ.2.0` anchor_to_reality); there are NO argument-less forms -- every occurrence carries a `<Name> <OP>...` tail. **Nissa's Pilgrimage's** (repo deck `foundations-tramplesaurus-rex`) `SVar:X:Count$Compare Y GE2.3.2` degraded to 0, so the search decision offered "choose up to 0 card(s)" and the spell always whiffed; that decision-bound defect is fixed here, and the full movement contract is now pinned end to end: `DBBattlefield`'s `ChangeType$ Card.IsRemembered` sub-search is a real hidden-library ask narrowed by the `IsRemembered` filter predicate (`effects/filter.go`), the mid-resolution Remembered set survives that ask's suspension (`effects/zone.go` `ResumeRemembered`, restored by `rules/resolution.go`'s resume arm), and `DBHand`'s object-valued `Defined$ Remembered` is a direct fetch list (`effects/zone.go` `moveDefinedLibraryObjects`) rather than a fresh hidden search -- one Forest lands on the battlefield tapped, the rest move to hand, both legs pinned in `rules/count_compare_test.go` (TestNissasPilgrimageSearchMaxFollowsSpellMastery) -- plus the corpus's SVar-named-branch singletons for free (the list above, all one occurrence each) and the **20** inline `CharmNum$ Count$Compare` Charm lines (**17** spell-side and **3** trigger-side `DB$ Charm` SVar bodies: disciple_of_perdition, soldier_military_program, you_exist_only_to_amuse), whose computed mode counts now resolve. | `effects/count.go` (`evalCompare`, `evalCountOperand`, `maxCountDepth`), `effects/filter.go` (`IsRemembered`), `effects/zone.go` (`effSearchLibrary` ride, `moveDefinedLibraryObjects`), `rules/resolution.go` (resume arm) | the 2 exotic singletons with no parseable threshold (`GEMePlus.3.2`, `LTZ.2.0`; M4 at the earliest) |
| `Vote`'s card-ballot shape is real but NARROW: a `VoteCard$` ballot is the battlefield permanents the filter admits (matched from the spell's controller), each `Defined$` player votes, every most-voted-or-tied permanent lands in the resolution's Remembered set, and `VoteSubAbility$` runs once against it (Council's Judgment's exile is real; pinned by `TestCouncilsJudgmentExilesTheMostVoted`). The **fixed-`Choices$` shape now resolves its winner**: each voter takes the first option (single shared helper `voteChoiceNames`/`voteWinner`), the Notes record it, and the winning option's SVar runs -- with `VoteTiedAbility$` chosen on a tie (`effVote` reads the param; pinned on the real Path of the Animist compiled `DBVote` SA by `TestPathOfTheAnimistTiedVoteRunsTheTiedBranch`, which answers a tie through `Ctx.Votes` and asserts the chaos branch runs, with a non-tied control on the same SA). `Ctx.Votes` is the answered per-voter choice list: the seam a real per-player ask will fill, consumed and cleared by `effVote` (fx42 scoping), and what makes the otherwise-unreachable tie branch testable against a real SA. Before this it ran NOTHING, so Path of the Ghosthunter's `DBPlaneswalk`/`DBChaos` outcomes never executed. The per-player vote CHOICE is still the deterministic no-ask stand-in -- every voter takes the ballot's first option, so the first option always wins unanimously -- and `VoteMessage$` (4 corpus files), `Secretly$`/`UpTo$` stay unread. | `effects/misc.go` (`effVote`, `effCardVote`, `voteChoiceNames`, `voteWinner`), `effects/registry.go` (`Ctx.Votes`) | M4 (a real per-player vote ask, which fills `Ctx.Votes` behind the same winner/tie logic) |
| The three planechase effect verbs are registered as no-ops with a Note: `api:BlankLine` (Forge's display spacer, SILENT -- no event) and `api:Planeswalk` / `api:ChaosEnsues` (CR 901.8/901.9), which record `"planeswalk (no planar deck)"` / `"chaos ensues (no planar deck)"` because this build has no planar deck and no planar zone (`Valid$ Plane` scans nothing; the DigUntil row's `PlanarDeck` carriers are the same gap). Registration is what stops `effects.Resolve` emitting its generic `"unimplemented API"` Note mid-chain; the real moves (next plane, planeswalk-away trigger, current plane's chaos ability) await a planar-deck tier. `Planeswalk`'s `Optional$`/`DontPlaneswalkAway$` and `ChaosEnsues$`'s `Defined$`/`Remembered$` riders are unread (there is no plane object to name). `T:Mode$ ChaosEnsues` (139 corpus files) is still unregistered and silently inert. | `effects/planechase.go` (`effBlankLine`, `effPlaneswalk`, `effChaosEnsues`, `notePlanechaseNoDeck`) | M4 (a planar-deck tier) |
| `BecomeMonarch` records the monarch designation through replayed `MonarchChange`, so `CheckDefinedPlayer$ You.isMonarch` (Regal Behemoth) is live; the monarch's end-step draw does not exist (Palace Jailer) | `effects/misc.go` (`effBecomeMonarch`), `events/apply.go` (`MonarchChange`) | M4 |
| A hidden-library `ChangeZone` search's minimum now splits on whether the card filter (ChangeType) states a QUALITY, per CR 701.23b/701.23d: a stated-quality search keeps `Min: 0` (the fail-to-find allowance, 701.23b) while a quantity-only search is forced to `Min = Max` clamped to the cards actually present, so it must find that many (701.23d). The split is `effects.SearchStatesQuality` (in `effects/filter.go`), a property of the filter — NOT of Forge's `Mandatory$`, which remains deliberately unread. **Measured population** (raw `.cards/cardsfolder` lines carrying `Origin$ Library` on a `ChangeZone`, parsed with `/usr/bin/grep` 3.11): **1329** such lines, of which **1175** carry an explicit `ChangeType$` and **154** omit it (defaulting to `Card`). Of the **1175** with `ChangeType$`, **1075** are stated-quality and **100** are quantity-only (`Card` 96, `Card.YouOwn` 3, `Card.YouCtrl` 1); adding the 154 default-`Card` lines, **254** quantity-only / **1075** stated-quality of 1329. The first row's earlier "always offers Min 0" claim is superseded by this split. | `effects/zone.go` (`effSearchLibrary`), `effects/filter.go` (`SearchStatesQuality`) | M4 (a real card-quality grammar for the fail-to-find predicate census) |
| `ShuffleNonMandatory$ True` asks its may-shuffle confirm since searchmay1 (`effects/zone.go` `searchShuffleTail`, resume arm `search_mayshuffle`), but ONLY when the search moved at least one card: the fail-to-find shape (nothing moved) keeps the CR 701.23d mandatory shuffle with no ask -- the AskEmpty pins' silence contract (Squadron Hawk's soft-lock), where Forge would pose the confirm too. The 3 raw lines where the flag rides an object-target `Origin$ Graveyard` `ChangeZone` are also still inert -- that path shuffles nothing at all today (a separate `Shuffle$` gap, not this flag's). | `effects/zone.go` (`searchShuffleTail`, `shuffleLibrary`), `rules/resolution.go` (`search_mayshuffle` arm) | M4 (a confirm on the fail-to-find shape, and the object-path shuffle it rides on) |
| Hidden-library `ChangeZone`'s reveal/look controls are implemented at the search path: `Reveal$` publicly reveals the found cards (one public Note carrying the moved ids, the Reveal primitive's payload, emitted after the moves), and a stated-quality search — `ChangeType$` beyond bare `Card`, destination not the battlefield, no `Defined$` — reveals by default (CR 701.23b/701.23d's must-reveal half), with `Hidden$ True` suppressing that default; `NoLooking$ True` offers card backs (every option label is the blind "a card" placeholder) to the searching player; `ForgetChanged$ True` drops each moved card from BOTH remembered halves (the resolution's ctx set and the source's event-backed list, one Choose `forget-remembered` event per card); `DifferentNames$ True` carries one option Group per card name — Decision.Validate's mutual-exclusion rule enforces it on the wire and `applyLibrarySearch` dedupes a host that bypassed the wire. The public-origin `Hidden$ True` pick (no `Defined$`, origin excluding Library/Hand and `All` — Kor Skyfisher's bounce, Temur Sabertooth's other-creature return, Relic of Progenitus's target-player graveyard exile, Burning Wish's Origin$ Sideboard, which holds nothing here so one loud Note says so and the SubAbility$ still runs) is a real pick ask (`ResumeKind "hidden_pick"`). **Still not implemented:** a LIMITED-look grammar (the chooser still sees and picks over the FULL eligible option list), and search decisions themselves remain private to the chooser — `view.project` attaches a decision only to `Decision.Player`; even `Omniscient` spectators receive no decision. Measured at the current corpus pin, exact-`Origin$ Library` raw lines: `Reveal$` 49, `Hidden$` 41, `NoLooking$` 53, `ForgetChanged$` 41, `DifferentNames$` 18 (corpus-wide, ForgetChanged$ 60, DifferentNames$ 22). | `effects/zone.go` (`effSearchLibrary`, `effHiddenPick`, `applyLibrarySearch`), `events/apply.go` (`Choose "forget-remembered"`), `view/view.go` (`project`) | M4 (limited-look grammar; spectator search decisions) |
| The R-9 no-host stand-in for an exact-`Origin$ Library` `ChangeZone` now answers for the absent player while staying CR-correct on both sides of the 701.23 split. A **quantity-only** search (`Min == Max`, the 701.23d shape that must find that many) takes the first `Min` eligible cards in the decision's own deterministic option order — or all of them when the library holds fewer (701.23d's "as many as possible"), so it still never wedges; a **stated-quality** search (`Min 0`, the 701.23b shape) still finds nothing, which is a legitimate fail-to-find. In both shapes the otherwise-required shuffle still happens, and the pick is byte-identical run to run because it comes straight from the ordered `eligible` list the decision's options were built from (no map iteration). The boundary with a real host is pinned: when `Host.Ask` returns true the search poses its KChoose decision, suspends, and the stand-in never runs (`TestLibrarySearchHostPosesDecisionAndStandInNeverRuns`). **Re-measured** over `.cards/cardsfolder` — exact-`Origin$ Library` `ChangeZone` lines counted with GNU `/usr/bin/grep` and cross-checked with a Python walker mirroring `SearchStatesQuality` (bare `grep` is ugrep here and honours `.gitignore`): **1329** such lines, of which **254** are quantity-only (`Card` 96 explicit, `Card.YouOwn` 3, `Card.YouCtrl` 1, plus 154 omitting `ChangeType$` and defaulting to `Card`) and **1075** state a quality — the previously-recorded 1338/1085/253 did not reproduce (the totals differ by 9 lines — 10 extra explicit-`ChangeType$` and one fewer default-`Card` — cause not pinned, but it is a counting-method/corpus-pin difference, not a behaviour one, and it does not change which shapes the fix touches). So the stand-in is now correct on both populations: nothing on the CR side remains of the old 701.23d violation. What is left is the generic R-9 approximation that the absent player's choice is the engine's deterministic first-`Min` pick, not a real one — the same fuzz/test degradation contract the `Charm` first-mode and `Scry`/`Surveil` rows carry, and the multi-player/mixed-origin narrowing lives in its own row. Pinned by `TestLibrarySearchNoHostQuantityFindsDeterministicFirst`, `TestLibrarySearchNoHostQuantityTakesAllWhenFewerThanMin` and `TestLibrarySearchNoHostFindsNothingAndShuffles` (the over-reach guard). | `effects/zone.go` (`effSearchLibrary`, `applyLibrarySearch`) | none -- the no-ask host is the fuzz/test degradation contract |
| Hidden-library `ChangeZone` moves the selected cards and honours `RememberChanged$`, `Tapped$`, and `LibraryPosition$`, but still ignores `GainControl$` (**13 raw exact-`Origin$ Library` lines**), `ExileFaceDown$` (**13**) and `Imprint$` (**14**). A fetched permanent therefore keeps its existing controller, an exiled card stays face up, and an imprint reference is not retained. | `effects/zone.go` (`applyLibrarySearch`) | M4 (control, face-down exile and imprint state) |
| A hidden-library `ChangeZone` whose `DefinedPlayer$` resolves to several libraries asks only the **first** library in deterministic resolution order. The live raw population is **11 exact-`Origin$ Library` lines** (`DefinedPlayer$ Player` x10, `Opponent` x1); restarting the same primitive after a suspended answer currently has no persisted per-player cursor, so asking again would re-ask player one rather than safely continue to player two. | `effects/zone.go` (`effSearchLibrary`, `searchPlayers`) | M4 (persisted per-player search continuations) |
| `ChangeType$ "EACH <A> & <B>"` (Forge's multi-type search grammar, 17 raw lines / 17 files) matches as a UNION and structures the hidden-library search's pick one-per-type: `eachAlternatives` splits the spec after the `EACH ` prefix on `&` into ordinary sub-specs, and matching, `SearchStatesQuality` and `UnknownPredicates` all read the split (before the fix the whole string was one never-matching base -- Krosan Verge et al. failed to find silently every time -- and the dotted form leaked the later clauses as garbage predicate tokens). `effSearchLibrary` builds ONE KChoose per EACH search with every per-type cap at most 1: options carry the type's ordinal in `Option.Group` (at-most-one per Group on the wire = one pick per type), Min 0 keeps CR 701.23b's fail-to-find allowance, and a listed type with no eligible card contributes no options and no Group. `botpolicy`'s "search" arm answers one first-option per Group up to Max. **Residual narrowness:** on the NON-library object paths (6 carriers: michelangelo_improviser, yunas_decision, druidic_ritual, eivor_wolf_kissed, sludge_titan, fugitive_of_the_judoon) the union matcher makes candidates reachable but the pick follows the ordinary count, not one-per-type; a per-type `ChangeNum$` above 1 (0 carriers) keeps the flat-count path behind a loud Note; a quantity-only EACH spec (0 carriers) keeps the mandatory-find reading of its sub-specs and the flat pick; overlapping sub-specs (one card matching two listed types) offer it in both Groups and picking it in one blocks the other (0 carriers). | `effects/filter.go` (`eachAlternatives`), `effects/zone.go` (`effSearchLibrary`), `botpolicy/policy.go` (the Group-aware `"search"` arm) | M4 (per-type pick structure on object paths; per-type ChangeNum counts) |
| Only the exact single-origin spelling `Origin$ Library` enters the hidden search path. The **17 raw lines / 13 files** whose origin is a compound containing Library (`Battlefield,Library`, `Graveyard,Hand,Library`, `Graveyard,Library`, `Library,Graveyard`, `Library,Hand`, `Library,Sideboard`) retain the pre-existing object-movement path; mixing a hidden library with public/sideboard candidates needs an origin-aware option API rather than silently exposing all candidates. | `effects/zone.go` (`effChangeZone`) | M4 (mixed-origin hidden searches) |
| An explicit multi-zone `ChangeZone Origin$` that includes `Hand` needs an origin-aware chooser: the exact-hand and exact-library walkers cannot build a single private option list across hand, graveyard, library or battlefield. `effChangeZone` detects the parsed zone set structurally (not by card or spelling) and emits one replay-visible Note before retaining the object path for an already-`Defined$` object; a source-default picker consequently moves nothing loudly rather than silently. **Measured:** 32 raw lines across 32 corpus files have an explicit multi-zone `Origin$` containing `Hand` (Kastral, the Windcrested's `Hand,Graveyard` Bird picker is pinned). | `effects/zone.go` (`mixedOriginIncludesHand`, `effChangeZone`) | M4 (origin-aware mixed hidden-zone chooser) |
| `RearrangeTopOfLibrary` poses a real `KArrange` decision over the top N cards and applies the answer as a single `events.LibraryOrder` (Ruling J0/J1): the chosen options, in answer order, become the new top of the library. The **no-host stand-in** (R-9, the host that cannot ask) keeps the existing order — pile A = the offered options in offered order — which is narrower than the card text's "put them back in any order". `MayShuffle$` (Ponder's "You may shuffle.", 5 raw lines) is READ since the final-stragglers sweep: the arrange re-entry pass poses a yes/no may-shuffle ask (the `arrange_mayshuffle` resume arm emits the Shuffle event before the chained SubAbility$ runs; the R-9 stand-in declines), so the row's remaining stand-in is only the multi-player `Defined$`/`ValidTgts$` rearrange that asks just the first library in scan order (a no-host run still applies the stand-in to every one). | `effects/cardflow.go` (`effRearrangeTopOfLibrary`), `rules/arrange.go` (`handleArrange`) | M4 (per-library asks) |
| `Scry` sends its unchosen pile B — the cards the player did NOT pick — to the BOTTOM in the OFFERED order, not the player's. CR 701.22 says "put any number of them on the bottom of your library in any order", but KArrange's answer only orders pile A (the cards kept on top); `handleArrange` builds pile B by scanning `d.Options` in index order, so the rejected order is the engine's, never the player's. The bottom pile's order IS later observable (those cards are eventually drawn), so this is a real divergence, not a cosmetic one. A multi-library `Defined$`/`ValidTgts$` Scry asks only the FIRST library: `effLookAndArrange`'s `if h.Ask(d) { return }` returns out of the whole `for _, t := range Defined(h, c, sa)` loop on the first ask, so a shape whose `Defined$`/`ValidTgts$` names more than one player silently drops the rest (the no-host stand-in path below still applies to every one). Measured over raw `.cards/cardsfolder` lines with `/usr/bin/grep` 3.11: **3** Scry lines carry a genuinely multi-player selector — `Defined$ Opponent` x2 (kaya_intangible_slayer, myr_custodian) and `Defined$ Player` x1 (eager_construct) — so those three name more than one player at resolution; the **2** `ValidTgts$ Player` lines (kozileks_command `ScryNum$ X`, bumi_king_of_three_trials `ScryNum$ 3`) name **one** chosen target (no `TargetMax$`, default 1), so they are single-library, not multi. | `rules/arrange.go` (`handleArrange`), `effects/cardflow.go` (`effLookAndArrange`) | M4 (a player-chosen pile-B order, shared with `Surveil`'s graveyard pile) |
| `Surveil` sends its unchosen pile B — the cards the player did NOT pick — to the GRAVEYARD in the OFFERED order, not the player's. CR 701.25 says "put any number of them into your graveyard" with no order specified, but the order the engine places them in the graveyard is the offered order, because `handleArrange` emits one `MoveZone` per pile-B card in `d.Options` index order. A `Surveil` has NO bottom pile (the fsv1 survey's bottom-pile reading is wrong; CR 701.25 governs), and there is no player choice over the graveyard pile's order. The same multi-library early-return applies as to `Scry` (the shared `effLookAndArrange` asks only the first library on a `Defined$`/`ValidTgts$` shape naming more than one player); measured over raw `.cards/cardsfolder` lines with `/usr/bin/grep` 3.11 the population is **0** — no `Surveil` line carries `Defined$ Player`/`Defined$ Opponent` or `ValidTgts$`, so the multi-library narrowing is corpus-unreachable for `Surveil` and only `Scry` carries it. | `rules/arrange.go` (`handleArrange`), `effects/cardflow.go` (`effLookAndArrange`) | M4 (a player-chosen graveyard order) |
| `Optional$ True` on `Scry` (4 raw corpus lines, e.g. "you may scry 2") is not read — the scry always happens. `Min 0` lets the player keep nothing on top, which is not the same as declining to look at all. No `Surveil` line carries `Optional$` (measured 0), so only the Scry side of the pair is narrowed. | `effects/cardflow.go` (`effLookAndArrange`) | M4 (optional scry) |
| `T:Mode$ Scry` (20 raw corpus lines) and `R:Event$ Scry` (2 raw corpus lines) do not fire. Registering `api:Scry`/`api:Surveil` does NOT register the "whenever you scry" trigger or the scry-replacement; nothing in this build may claim it does. | `effects/cardflow.go` (`effScry`, `effSurveil`) — no `trig:Scry`/`repl:Scry` is registered in `rules/` | M4 (scry triggers and replacements) |
| The R-9 no-host stand-in for `Scry`/`Surveil`: a host that cannot ask keeps every card on top in its existing order — for `Scry` pile B is empty (nothing goes to the bottom), for `Surveil` nothing goes to the graveyard — recorded as one `events.LibraryOrder` carrying the unchanged order, so the resolution completes rather than wedging. This is narrower than the card text (the player never actually chooses) and a fuzz/no-host run hits it. | `effects/cardflow.go` (`effLookAndArrange`) | M4 (a real host for every seat) |
| `Dig`'s look-and-take now asks where a choice exists (dig1): when the top `DigNum$` window holds STRICTLY more `ChangeValid$`-eligible cards than `ChangeNum$` — effDiscard's `TgtChoose` strict-supersets rule, so a decision nobody could answer differently is never emitted — `effDig` records the look as a Secret Note carrying the window's ids (only the library's owner may know it; redaction rule (1)) and poses a real `KChoose` to the owner over the ELIGIBLE cards only (an ineligible card is not pickable), Min 0 when `Optional$ True` or `ChangeNum$ Any`, else `ChangeNum`, Max `ChangeNum`, answer re-entered through `ResumeKind "dig"`/`Ctx.Dig`, `botpolicy` taking the first Max options (the exact mirror of the no-ask stand-in, so no event stream changes for a bot-answered game). The ask gate ALSO requires `changeNum > 0` (r2): a `ChangeNum$ 0` dig takes nothing, so with eligible cards it must not pose the Min==Max==0 KChoose whose only legal answer is the empty one — measured, exactly **3** corpus files carry a zero-cap Dig (birthing_ritual, sanity_grinding, stomping_slabs, all reveal-machinery shapes whose `RememberRevealed$`/`Reveal$` are themselves unread; the silent no-op take is correct for them). The no-host fallback's Secret Note carries `Player` = the library's owner (r2: without it a Dig of another player's library routed the private note to seat 0). The dig-variant params (digvar1): `Reveal$` (a public ids-Note of the window before any take, replacing the ask path's private look; 4 deck/corpus carriers), `NoReveal$` (a guard — the moves were always Secret), `ForceRevealToController$` (each MOVED card is revealed publicly before its Secret move — Ancient Stirrings' "you may reveal a colorless card"), `Tapped$` (a MoveZone-then-Tap pair on a battlefield take, the effChangeZone shape), `DestinationZone2$`/`LibraryPosition2$` (the remainder's second destination: every window card the primary move did not take goes there — Chaos Warp/Goblin Guide's unmatched card back to the library at position "0" = top, which is the engine's stay-in-place default so no event is emitted; "-1" = a real library-to-library move to the bottom, 4 raw lines) and `SkipReorder$` (the engine's remainder contract itself; also suppresses a DestinationZone2$ placement) are READ. `ChangeNum$ Any` (40 raw lines) is now the any-number ask it names — an earlier note here claimed it "falls into the take-all default"; measured FALSE, the unresolvable-value Num read degraded Any to 0 and Jace TMS/Through the Forest Gate's digs took NOTHING (Jace's whole [2] loyalty ability was a no-op); it now caps at the window and lowers the ask's Min to 0 whenever an eligible card exists. The Dig `ChangeValid$` window is also rewritten through `permanentCardSpec` (a leading `Permanent` base → `PermanentCard`): matchesBase reads a bare Permanent as an ON-THE-BATTLEFIELD object, so Chaos Warp/Matter Reshaper's `ChangeValid$ Permanent…` never matched a LIBRARY card before and those digs took nothing (measured: this is the 4-seat golden head's movement — seat 2's Matter Reshaper death trigger now reveals the dug card and puts a matched one onto the battlefield); rules/stack.go's `targetSpecForZone` carries the twin rewrite for target specs. Everything else keeps a stand-in: an `Optional$ True` window with exactly `ChangeNum$` eligible cards still plays "you may take" as a mandatory take (the no-choice path emits nothing, so such games replay byte-identically to the pre-dig1 engine); the remainder decision ("the rest on the bottom in any order") is not asked — the untaken cards stay ON TOP in their existing order, with `RestRandomOrder$` unread (205 raw lines); the PRIMARY `LibraryPosition$` (96 raw lines put the primary take at a library position), `Choser$` (the opponent-chooses planeswalker shape, 9 raw lines) and `DigNum$ X` (115) are unread; a Dig resolved on the no-choice path records no look at all; a multi-player `Defined$` Dig asks only the first library with a real choice; re-entry tracks that target index, skips earlier libraries already processed before suspension, applies the answer only to the asking library, and processes every later library with the former deterministic first-eligible stand-in rather than abandoning it. Chained per-library asks remain unimplemented. Counts are raw `.cards/cardsfolder` lines matching `\b(SP|DB|AB|ST|RE)\$ Dig\b` = **989** across **947** files (514 with a look window > 1; 243 the Ancient Stirrings pick-1-of-N shape), re-measured with GNU `/usr/bin/grep` at the dig1 gate. Repo-deck note: `tron.json` carries Ancient Stirrings, but `tron` is `legacyDeckNames` index 8 and the acceptance games seat indices 0–7 at every gate seat count, so no golden game casts it and `TestHeads` is safe by construction (measured: 0 Ancient Stirrings casts, 0 Dig asks in all four golden games). | `effects/cardflow.go` (`effDig`, `digDestPhrase`), `effects/registry.go` (`Ctx.Dig`/`DigDone`/`DigTarget`), `rules/resolution.go` (`resumeResolution` "dig" arm), `rules/turn.go` (`handleChoose`), `botpolicy/policy.go` (the `"dig"` arm) | M4 (per-library asks; remainder ordering; `Optional$` on the no-choice path; the primary `LibraryPosition$`; `Choser$`; `DigNum$ X`) |
| `api:DigUntil` is REGISTERED and its core shape is real (diguntil1): reveal cards from the top of `Defined$`'s library (default: the resolving controller; `ValidTgts$`-targeted players resolve through `Defined`'s target fallback) in zone order until the `Amount$`-th `Valid$` match (`Amount$` literal 1..5 honoured, default 1), publicly reveal every card turned over INCLUDING the found one (the same non-Secret ids-Note `effDig`'s `Reveal$` arm emits, recorded once per resolution — the re-entry after the optional-move answer must not re-reveal), move the found card(s) to `FoundDestination$` (default Hand) and the revealed rest to `RevealedDestination$` (default Library; `RevealedLibraryPosition$` "-1" = bottom via Move's zone append, "0"/absent = the stay-in-place default so no event), and leave every card AFTER the found card untouched. The scan is `permanentCardSpec`-rewritten like `Dig`'s window. `OptionalFoundMove$ True` (4 corpus lines — Songbirds' Blessing) poses a real yes/no `KChoose` to the library's owner (`ResumeKind "diguntil_move"`, answered through the new `Ctx.DigUntilMove`/`DigUntilMoveDone` fields consumed-and-cleared at the top of the walk, fx42): "yes" moves to `FoundDestination$`; "no" — the decline — moves to `OptionalNoDestination$` when the SA carries one (exactly 1 line), else the found card JOINS the revealed pile — the corpus oracles all say "then put all cards revealed this way that weren't put onto the battlefield on the bottom" (Genesis Storm, Hei Bai, Aurora Awakener), which is NOT the brief's "else FoundDestination$" reading; measured and corrected. The no-host (fuzz/no-engine) declines deterministically (R-9); a bot answers option 0 = "yes" through botpolicy's clamp fallback (no dedicated arm — the attach_optional precedent). Riders read: `RememberFound$` 59 / `RememberRevealed$` 22 (ctx-level Remembered — pinned end to end on Ketria's real compiled RolledChaos, whose chained `Defined$ Remembered` ChangeZone moves the found card onto the battlefield), `Tapped$` 8 (the MoveZone-then-Tap pair), `GainControl$` 9 (the found permanent enters under the resolving controller via the ordinary ControlChange), `NoMoveRevealed$` 8 (the revealed cards stay where they are). A found card with an AURA face put onto the battlefield gets the CR 303.4f non-cast-entry attach, degraded to the deterministic stand-in: the first permanent in the controller's battlefield zone order satisfying the Enchant keyword's own spec (`MatchesSpecFrom`, the same read rules/attach.go's auraStillMatchesEnchant does); with NO eligible bearer the Aura stays in the library (CR 303.4f) instead of dying to the 704.5m SBA — population: exactly 1 corpus line (Songbirds' Blessing itself). **The `RevealRandomOrder$` stand-in** (54 lines, every occurrence "True"): the revealed pile would return "in a random order" — randomness is forbidden here, so the cards return in their existing library order (the deterministic bottom moves) and the row stays open for it. **Withheld, loudly** (each emits ONE Note naming the param and the core move runs without it; measured over the 169 raw SA lines): `Amount$` non-literal X 8 / MassX 2 / Y 1 / VoteNum 1 (amount 1 then), `DigZone$` 4 (only Library is a real zone — the PlanarDeck/`Valid$ Plane` carriers scan NO zone and move nothing), `NoMoveFound$` 1 / `FoundLibraryPosition$` 4 (the found card stays where it is — all position "0" = already on top), `Shuffle$` 19 / `ShuffleCondition$` 1 (the revealed rest go to RevealedDestination$ in existing order instead of shuffling in), `ImprintFound$` 15 / `ImprintRevealed$` 6 (no imprint association is recorded), `NoneFoundDestination$`/`NoneFoundLibraryPosition$` 1 (no-match still moves the revealed cards to RevealedDestination$), `ForgetOtherRemembered$` 3, `MinTotalCMC$` 3, `NoPutDesc$` 4, `Reveal$` 5 (the reveal-until reveals by nature; the param is unread), `ValidPlayer$` 1 (Ketria's leftover — inert). `IsCurse$` 15 / `Cost$` 31 / `ValidDescription$` 114 / the description params are display/activation metadata the engine never reads on any API. Measured population (GNU `/usr/bin/grep`, re-run at this gate): **169** raw SA lines matching `\b(SP|DB|AB|ST|RE)\$ DigUntil\b` across **165** files; `make report` moved `cards: 33667 playable: 26001 (77.2%)` → `26112 (77.6%)` and `api:DigUntil` (was #6, 146 cards unlocked) off the top-missing list. Songbirds' Blessing is in NO repo deck and NO legacy golden deck, so `TestHeads` and the ratchet are safe by construction and verified unchanged. | `effects/cardflow.go` (`effDigUntil`, `digUntilParamValue`, `auraEntryBearer`), `effects/registry.go` (`Ctx.DigUntilMove`/`DigUntilMoveDone`), `rules/resolution.go` (the `"diguntil_move"` arm), pinned by `effects/diguntil_test.go` and `rules/diguntil_songbirds_test.go` | M4 (the `RevealRandomOrder$` pile order; the withheld list above; a real bearer choice for the CR 303.4f Aura attach) |
| The tap gate is colour-aware for plain `Produced$` strings, but `Any`/`Combo Any` still resolve to colourless, and `Combo X Y`/`Chosen` retain the executor's degenerate rune output rather than a selectable colour. The land drop (`chooseLand`) now ranks **colour coverage first** -- how much of the seat's still-unmet coloured-pip demand, net of the pool and the guaranteed production of every source already on the battlefield, a candidate land's production covers -- then **basic-ness as a tiebreak**, then **fewest distinct colours** (play the least-flexible, keep the flexible source in hand) | `effects/misc.go` (`effMana`); `botpolicy/cast.go` (`chooseLand`) | M4 mana-choice decisions |
| The tap gate's production collector no longer prices a non-literal `Amount$`: it returns an **unknown** amount, the source gains `Indeterminate`, and the ability contributes **zero** colour slots -- so a source does not advertise mana the pool is never promised (`Num`'s `X`-or-0 read as a real colour slot is gone). Measured on the corpus pin, populations explicit. **Raw lines** (loose `/usr/bin/grep -rF 'AB$ Mana'` over `.cards/cardsfolder`, matching both `Mana` and `ManaReflected`): **122** raw `AB$ Mana` lines carry a non-literal `Amount$` (X 109, Y 7, UrzaAmount 3, plus three one-offs -- `Sacrificed$CardPower`, `IncubationAmount`, `Count$Valid Goblin.YouCtrl`); **53** of them pair it with a plain single-letter coloured `Produced$` (G 24, R 9, B 9, U 8, W 3). **Compiled SAs** (the `Face.ManaAbilities()` input the collector actually reads, via `cards.OpenCorpus`): **116** non-literal-amount abilities (X 105, Y 7, UrzaAmount 3, `Sacrificed$CardPower` 1 -- no `IncubationAmount`/`Count$Valid` reaches `ManaAbilities`), of which **49** pair with a plain single-letter coloured `Produced$` (G 23, R 7, U 7, B 9, W 3), or **57** counting the `Combo X Y` shapes whose degenerate rune walk still emits coloured slots (49 + 8). **Operational, per face** (within the subset of the **116** faces that have a non-literal-amount mana ability): **58** had `ProducesColour` true before the fix, **3** do now (Crypt of Agadeem, Glittering Stockpile, Itlimoc, Cradle of the Sun -- each keeps it via a separate literal-amount coloured ability), so **55** faces stop claiming a colour they cannot deliver. Still unfixed: `Any`/`Combo Any` resolve to colourless, and `Combo X Y`/`Chosen` carry the degraded rune output rather than a selectable colour. Only zero-vs-nonzero reaches the decision -- the magnitude never does -- and all real production still comes from `effMana` via `events.Apply`, so this is a bot-quality improvement, not a state-correctness one | `cards/mana_production.go` (`manaAbilityAmount`, `add`) vs `effects/count.go` (`Num`); `effects/misc.go` (`effMana`) | M4 mana-choice decisions |
| Bot target selection's damage consumer trusts a supplied amount without inspecting the API string: `effectDamage` (`botpolicy/target.go`) casts `d.TargetEffect.Damage.Amount` and reports it as known damage whatever the API. That is a **consumer-contract discrepancy, not reachable in play** -- the engine only ever populates `DamageEffect.Amount` for `DealDamage`/`DamageAll` (`describeTargetEffect`), so an unknown API never carries an amount for the consumer to misread. As implemented, ranked by the ft1 tiers: (1) an opposing creature that may kill the seat this round and that the damage can actually kill (first), (2) an opponent's face when `amount >= life` -- potentially lethal (second), (3) the most powerful opposing creature the damage kills -- but only when the seat has spare floating mana (third). An absent `target_effect` or a null/absent amount keeps the plain board-over-face ranking (neutral face constant), so an unreadable effect is never lethal and never a threat; "spare" is read only from the pool (`hasSpareMana` = non-empty pool), never assumed from untapped sources, which the Board does not carry. Every other effect (Destroy, counter, non-damage) stays effect-blind, and a face the known damage cannot reach also ranks below creatures. | `botpolicy/target.go` (`effectDamage`/`mayKillMe`/`hasSpareMana`/`effectRanker`/`chooseTargets`) | richer outcome modelling / Board mana-source facts |
| A host that cannot answer a decision gets the deterministic fallback: `Charm` takes its first mode with a Note (the modes ask itself is a real KModes decision since M2d-2) | `effects/misc.go` (`effCharm`'s fallback) | none -- the no-ask host is the fuzz/test degradation contract |
| `MatchesPlayerSpec` (rebuilt as fx20) honours a player spec's qualifier and **fails closed** on any it does not list: a base `Player`/`Any` whose qualifier is not one of `You`/`Opponent`/`Other` returns false, and a qualified `You`/`Opponent`/`Other` (e.g. `You.descended`, `Opponent.NonActive`) also returns false, so the narrow-but-real qualifiers the corpus actually uses -- `NonActive`, `EnchantedBy`, `EnchantedController`, `Chosen`, `IsRemembered`, `IsTriggerRemembered`, `descended`, `TriggeredDefendingPlayer`, `withMoreLandsThanYou`, and the `+`-compound and space-bearing forms (`Opponent+Active`, `Other+wasAttackedThisTurnBy Assassin.YouCtrl`) -- now match **nobody**, i.e. the trigger never fires. Three state-local qualifiers ARE evaluated, on any of the `Player`/`Any`/`You`/`Opponent`/`Other` bases (the base still applies): `Active` (the active player -- `Player.Active`, `You.Active`), the life-total comparisons `life<GE|GT|EQ|LE|LT><N>` (`You.lifeGE1`, `You.lifeGE7` on LifeLost triggers), added with the draw/life trigger primitives, and -- added with the static GainControl reader (2026-09-20, agent-20260918T200326Z-4953f078) -- `isMonarch` (CR 716.2's monarch designation), evaluated on the `Player`/`Any` base ONLY: a qualified `You`/`Opponent`/`Other` base (`You.isMonarch`) still fails closed. That retired `Player.isMonarch` from the outstanding list: the corpus's six `Player.isMonarch` carriers (Fealty to the Realm's `GainControl$`, Archivist of Gondor's Phase `ValidPlayer$`, Okoye/MBaku/The Spear of Bashenga's attack conditions, Azure Fleet Admiral's CantBlockBy `ControlledBy`) now resolve through the shared grammar where the MatchesPlayerSpec-fed paths read them. Before fx20 (`92e63a2`) `case "Player","Any": return true` ignored the qualifier entirely, so these specs fired for ANY player (too permissive). fx20 traded too-permissive for dead; a never-firing trigger is a different divergence from a wrong-wide one, not necessarily a smaller one. CR 603.4/603.10: a trigger fires only when its condition is met, so a player-qualified trigger whose qualifier the engine cannot yet evaluate should fire against the qualifying player, while this build keeps it permanently silent. **Do NOT implement the missing qualifiers** -- `EnchantedBy`, `Chosen`, `TriggeredDefendingPlayer`, the `IsRemembered`/`controlsCard`/`counters_` numeric forms and the `+`-compound/space forms need player-state machinery out of scope here; this row records the measured size of the hole. **Measured** over raw `.cards/cardsfolder` lines with GNU `/usr/bin/grep` 3.11 (bare `grep` in this shell is ugrep and returns nothing over the gitignored corpus) and a Python walk. These are RAW-LINE / distinct-spec counts -- an **upper bound** on affected script sites, not a count of cards that misbehave in play (many such triggers also sit behind unimplemented machinery: the two `Player.EnchantedBy` `DamageDone` lines also carry `CombatDamage$ True` -- which `damageMatches` now honours -- but the `Player.EnchantedBy` target spec still matches nobody, so they still never fire). At the five call sites that feed `MatchesPlayerSpec` (`spellCastMatches`' `ValidActivatingPlayer$`, `phaseMatches`' `ValidPlayer$`, `damageMatches`' player-target `ValidTarget$`, `actorMatches`' `Caster$`/`Activator$`, `restrictionActorMatches`' `Activator$`) the corpus carries unhandled-qualifier player specs on **69 distinct spec strings across 187 distinct cards** (70 across 188 before `isMonarch` was evaluated, 72 across 194 before `Active` was; that retired `Player.Active` and `You.Active`, 6 cards: The Council of Four's `ValidActivatingPlayer$`, the `Caster$ Player.Active` of Jace Reawakened, Serra Avenger, Spider-Man 2099 and The Keeper of the Yellow Hat, and Professor Hojo's `Activator$ You.Active`). Scoped to the three trigger modes the engine routes through `MatchesPlayerSpec` (`SpellCast`, `Phase`, `DamageDone`): **17 distinct spec strings across 86 distinct cards** (18 across 87 before `isMonarch`) -- `SpellCast` 6 (`Player.NonActive` x4, `Player.EnchantedBy` x5, plus one each `Player.Chosen`, `Player.Active+!IsRemembered`, `Player.Opponent+Active`, `Opponent.NonActive`), `Phase` 6 (`Player.EnchantedController` x34, `Player.EnchantedBy` x13, `You.descended` x10, `Player.Chosen` x3, `Opponent.EnchantedBy` x2, plus one `Player.Other+!IsRemembered`), `DamageDone` 8 (`Player.TriggeredDefendingPlayer` x2, `Player.EnchantedBy` x2, plus one each `Player.attackedYouTheirLastTurn`, `Player.controlsLand.counters\_GE1\_CONTESTED`, `Player.counters_EQ0_Contract`, `Player.wasDealtCombatDamageThisTurnBySourceTimes EQ1`, `Player.wasDealtDamageThisTurnBySource GE10`, `Player.withMoreLandsThanYou`, plus one `Mode$ DamageDoneOnce` line (`Player.EnchantedBy`), routed to `damageMatches` since fb-20260914T145022Z-64a8422c but still silent because the spec matches nobody). Restriction keys `Caster$`/`Activator$` account for a further **16 distinct spec strings across 39 distinct cards** | `effects/filter.go` (`MatchesPlayerSpec`), `rules/trigger_match.go` (`spellCastMatches`, `damageMatches`, `phaseMatches`), `rules/statics.go` (`actorMatches`), `rules/layers.go` (`restrictionActorMatches`) | M4 (a full player-spec grammar and the player-state machinery `NonActive`/`Active`/`EnchantedBy`/`Chosen`/`descended`/`Remembered`/`counters_` need) |
| `ValidLKI$` on a replacement is honoured but **fails closed for the may-play provenance it gates**, not because there is no cast-from-graveyard path — `TestFlashbackCastsFromTheGraveyardPaysASacrificeAndExiles` proves a flashback cast from the graveyard is real. The gate evaluates `MatchesObjectCtx` against Forge's `Card.CastSa Spell.MayPlaySource` style spec, and the `MayPlaySource`/`CastSa` predicates are not implemented (unknown predicates fail closed), so the gate never admits a conditional graveyard-cast replacement (the Eelectrocute / Glimpse the Cosmos family). The conservative direction is still correct — an ordinary hand-origin cast of such a card must finish in the graveyard, which `TestCR608CorpusOrdinaryBurnFinishesInGraveyard` pins — but the POSITIVE route (a graveyard-origin cast actually triggering the may-play replacement) is unproven, not working | `rules/replacement.go` (`replacementMatches`) | the may-play cast-provenance predicates (`MayPlaySource`/`CastSa`) in the ValidLKI gate |
| A competition of all-`Updated` replacements applies them in deterministic scan order with **no CR 616.1 order choice**. That is correct where the replacements commute (the shape the CR 614 leaf measures, which tolerates either order) and wrong where they do not; no leaf exercises a non-commuting Updated competition. The order choice IS posed when the competition is not all-Updated, and for every competition of life gain/loss replacements (`R:Event$ GainLife`/`LifeReduced`, `applyLifeReplacements`) except one whose members are all the same commuting operator (all `ReplaceCount$.../Twice`, all `/Plus.N`, all `Prevent$ True`). A non-commuting life competition met while another decision is already outstanding, or affecting a player who has left the game, applies in scan order instead, since a second ask would overwrite the first | `rules/replacement.go` (`composeUpdatedReplacements`, `poseLifeReplacementChoice`) | M4 (order choice for every competing shape) |
| A bot or a host that cannot answer takes **option 0** of the new `KReplacement` order decision, through `botpolicy`'s clamp fallback rather than a policy arm of its own. Deterministic and non-wedging (verified: a new decision kind with no arm is how a headless game hangs), and the same class of stand-in as the `Charm` first-mode default | `botpolicy/policy.go` (`Decide`'s clamp fallback) | M4 (a real replacement-order policy) |
| The CR 704.5j legend rule keeps the **first** duplicate in battlefield scan order and bins the rest, with **no controller choice**. CR 704.5j gives the controller the choice of which one to keep; this build has no player-facing chooser for a state-based action (an SBA is not a decision a seat answers), so the survivor is picked deterministically by scan position. The invariant the conformance leaf certifies -- two same-named legendaries under one controller never both reach priority -- holds either way, but WHICH one survives is the engine's, not the player's. Correctly scoped per controller: two players may each keep their own copy | `rules/sba.go` (`legendCasualties`) | M4 (a controller choice for SBA-driven sacrifices, shared with the 704.5m/704.5q families) |
| **Monocolour** hybrid (Forge's `2B` shape: two generic OR one black, CR 107.4e) still degrades to a single generic pip, so it is priced at 1 instead of 2-or-a-colour -- under-priced by one generic, and payable by a colour it should also accept. Two-colour hybrid (`GW`, `W/U`) and Phyrexian (`BP`, `UP`) ARE now represented as real alternative payments, so this row is what is LEFT of the old flatten-everything-to-generic approximation, not the whole of it. **Measured** over `.cards/cardsfolder` with GNU `/usr/bin/grep` (bare `grep` here is ugrep and returns nothing over the gitignored corpus): **19 cards** carry a monocolour hybrid pip -- the Khans "Archaic"/Ascendancy cycle, Beseech the Queen, Kin-Tree Severance, Monastery Messenger and the wedge legends. Deliberately not widened: the generic-or-colour hybrid needs a third `Cost` shape (a generic ALTERNATIVE, not a colour pair) and none of the CR 107.4e leaves exercise it | `rules/mana.go` (`ParseCost`'s two-character hybrid branch) | M4 (a generic-or-colour hybrid alternative alongside `Hybrid []ManaPair`) |
| A modal spell now announces modes during CR 601.2b and a one-mode Charm chooses targets from that announced mode before payment. When `CharmNum$` selects several independently targeted modes, however, the stack object still has one undivided target list: casting uses the first selected target-bearing mode's declaration, and every selected mode receives that shared list at resolution. A REPEATED target-bearing mode (`CanRepeatModes$ True`, CR 601.2b) is the closed slice: only the mode's FIRST occurrence inherits the shared announcement/placement list (effCharm's covered-marking is first-occurrence-only, seeded across charm-rest resumes by `Ctx.ModesSeen`), and every later instance poses its own targeting ask (the mvts1 pre-ask / changeZoneChosenTargets, with the root `TargetsOffered` marker shed for that dispatch) — pinned per instance on Mystic Confluence in `rules/charm_repeat_modes_test.go`. Distinct target-bearing modes still share the first one's list. | `rules/cast.go` (`castModeAsk`, `modalTargetSA`), `rules/stack.go` (`resolveTop`), `effects/misc.go` (`effCharm`'s per-instance scoping), `rules/resolution.go` (the charm_rest arm's ModesSeen seed) | per-mode target groups (the distinct-mode half) |
| `non<X>` predicate negation is now generic: for `<X>` a type/supertype/subtype word in the corpus type vocabulary (`effects/typewords.go` `predicateTypeWords`) or one of the five colours (`White`/`Blue`/`Black`/`Red`/`Green`, via `ColorsOf`), `non<X>` is the negation of the positive form. This is what Walk the Plank's `Creature.nonMerfolk` (a subtype, previously matched NOTHING because only `nonLand`/`nonCreature`/`nonBasic`/`nonBlack` were hand-written) and a colour `Creature.nonBlue` need; the four handwritten entries stay and the generic path reproduces each exactly, so they could be deleted without behaviour change. An `<X>` that is neither a type word nor a colour still **fails closed** (the spec never matches), which is the safe direction -- `!hasType` on an unknown word would silently always match and widen the filter. The three remaining narrower shapes are `nonColorless` (a colour-*identity* test `ColorsOf` empty vs non-empty, **28** raw `.cards/cardsfolder` lines), `nonChosenCard` (**29**) and `nonCopiedSpell` (**1**) -- each `<X>` is neither a type word nor one of the five colours, so a card carrying one still has that target spec match nothing. | `effects/filter.go` (`nonPredicate` + the predicate loop), `effects/typewords.go` (`predicateTypeWords`) | M4 (a colourless/identity test; the Chosen/Copied stack-object predicates) |
| The positive counterpart of `non<X>` landed (pc1): a predicate word that is a type/supertype/subtype word in `predicateTypeWords` evaluates as `hasType`, and `Colorless`/`MultiColor` read off `ColorsOf`, so `Land.Basic` (a Forest), `Creature.Artifact` (an artifact creature), `Creature.Colorless` (a Devoid creature), `Card.MultiColor` (a gold card) now match. `UnknownPredicates` shares the same classifier (`wordPredicate`) as the matcher, so the two cannot disagree; the handwritten `Legendary`/`Snow`/colour entries in `predicates` are consulted first and still win, and were left in place (their result is byte-identical to the generic path, so deleting them is behaviour-neutral). **What still fails closed**: an unknown `<X>` that is neither a type word nor `Colorless`/`MultiColor` still matches nothing and is still reported. The context predicates the remembered/triggered-referents wave named ARE read on the positive (and `!`) path: `IsRemembered`/`!IsRemembered` (the resolution's Remembered set UNIONED with the source's persistent event-backed Remembered, both fail-closed to no-match when empty), `greatestPower[ControlledBy <players>]` (ties match), `ThisTurnEntered[_From_<Zone>]` (the per-object entry provenance `events.Move` records), and the name-predicate family `named<Name>`/`notnamed<Name>`/`sameName` (with the Remembered./Targeted./Triggered. base-prefix referents) -- each pinned in `effects/bangpredicate_test.go` / `rules/remembered_referents_test.go` on a real corpus card. Measured by the census walk (raw `.cards/cardsfolder` `ValidTgts$`/`ValidCards$`/`ChangeType$` specs only, comma alternatives split) the outstanding unknown predicates dropped from **573 distinct / 2575 card-occurrences** pre-wave to **424 distinct / 1081 uses across 983 card files** post-wave, and the largest remaining families (card files, uses in the live census) are `ExiledWithSource` (72, 82), `wasDealtDamageThisTurn` (28, 32), `IsImprinted` (28, 29), `NotDefinedTargeted` (23, 23), `DefenderCtrl` (18, 18), `Opponent` (15, 15) -- every one of these needs object or game context (exile/craft provenance, combat history, imprinted lists) that this predicate path does not carry, so they still match nothing and are still reported by `UnknownPredicates`. | `effects/filter.go` (`wordPredicate`/`wordMatches` + the positive branch in `MatchesObjectCtx`/`UnknownPredicates`) | M4 (object/game-context predicates: LKI, zone-history beyond ThisTurnEntered, imprint) |
| Static P/T with variable values and layer-4-derived type applicability are REAL but their layer-4 reach is confined to the layer walk. Static `AddPower$`/`AddToughness$`/`SetPower$`/`SetToughness$` values go through `effects.Num` (the shared signed-literal / SVar-name / `Count$` grammar) at EVERY derivation, so a life total, counter or zone change moves them without a cached snapshot; the value is add-saturated at the int32 boundary (Forge's calculateAmount bound), and a static that sets only one characteristic leaves the other alone (`SetPowerPresent`/`SetToughnessPresent`; a `CharacteristicDefining$` setter lands in the CR 613.4a CDA sublayer, so Tarmogoyf and Krovikan Mist announce their computed P/T). Measured corpus populations (raw `S:` lines): `AddPower` 294 / `AddToughness` 211 non-literal of 1938/1532 total, `SetPower` 241 / `SetToughness` 182 of 381/322 -- dominated by bare `X`/`-X` SVars plus 33 `AffectedX` and 12 `Y`; unresolvable bodies (an SVar name the face does not define) still degrade to 0 as before. Type applicability inside the walk: `matchesWithTypes` binds `SpecContext.ExtraTypes`, the types-so-far list -- a layer-7 lord's `Affected$ Goblin` and a later layer-4 effect both see a type an earlier layer-4 effect granted (Kudo/Beorn pinned). **Ordinary filters outside the walk still read the printed face plus Changeling's CDA**: target offer and legality, cost-candidate sites, Count$Valid in rules-built contexts and CantTarget restriction specs do not see a layer-4 grant (a creature a static made a Goblin is counted by a static P/T SVar or lord but is not targetable by a Goblin-killer). Changeling itself is real everywhere: `hasType` falls through to the positive `creatureSubtypeWords` vocabulary (Arcane/Alara/Ajani correctly excluded), so Goblin Piledriver counts an attacking Changeling Outcast. A CALLABLE resolver field on `SpecContext`/`Ctx` was measured to poison escape analysis -- the specCtx Resolve closure heap-allocates on every hot-path construction even when the field is nil at runtime, which the cost/action-statics hotspot pins hold the line on -- hence the value-slice walk plumbing. The S:-static type-strip flags are now read at the layer-4 walk too (Darksteel Mutation ticket 2026-09-18T232601Z): `staticEffects`' AddType block emits `RemoveCardTypes$`/`RemoveCreatureTypes$` (new `state.ContinuousEffect.RemoveCardTypes`, strip-only statics still emit), and `typeCharacteristics` strips before that effect's own `AddTypes` -- RemoveCreatureTypes$ drops every subtype word, RemoveCardTypes$ keeps only the supertypes (subtypes are tied to their card types, CR 205.2-family; both flags together = "everything but supertypes", Darksteel Mutation's oracle). Measured: 41 distinct carrier files (RemoveCardTypes$ 24, RemoveCreatureTypes$ 27, all also carrying AddType$), none in any repo deck -- heads and the ratchet unmoved. The same ticket fixed a same-line layer-6 ordering defect the carrier exposed: the `active()` sort now orders a full-tie (same layer/sub/timestamp) `RemoveAbilities` removal BEFORE a grant, so a static line carrying both `RemoveAllAbilities$ True` and `AddKeyword$` (measured 4 files: darksteel_mutation, deep_freeze, stasis_field, spider_man_no_more) grants its keyword instead of having it wiped by its own removal; timestamps still dominate (pinned). | `rules/layers.go` (`staticEffects`, `staticAmount`, `typeCharacteristics`, `matchesWithTypes`), `effects/filter.go` (`hasType`, `hasTypeCtx`, `typePredicate`), `effects/creature_types.go` | M4 (layer-4 types in the ordinary filter grammar: target offer, cost sites, Count$Valid) |
| The four trigger modes the Marvel Super Heroes Commander import needed are implemented (trig:AttackersDeclared, trig:Cycled, trig:CounterAdded, trig:AttackerBlocked, plus the PresentZone$ clause; pinned on real corpus cards in `rules/msh_commander_trigger_test.go`), with these measured limits. **AttackersDeclared** fires once per the engine's per-defender `DeclareAttackers` event, so a SPLIT attack (one declare step, several defenders) fires a batch "whenever you attack" trigger once per attacked player, not once per declare step -- Forge batches the whole declaration. **Cycled** recognises a cycle as the cost-discard marker (`events.DiscardCost`) of a card whose PRINTED face carries `K:Cycling` -- a card whose cycling is granted in a layer never matches, and a printed-cycling card discarded as the cost of a DIFFERENT card's ability false-fires the trigger (both measured corpus shapes; the second is rare and shares the provenance ambiguity the existing Sacrificed/Discarded cost triggers accept). **CounterAdded**'s `CounterAmount$` is the CROSSING gate the oracle text means (below n before the put, at least n after: the tenth counter fires on 9+1, on a single 10, on a single 11 from below and on a 9+2 overshoot; never on 7+1, 11+1 or later puts) -- the threshold ops EQ/GT/GE collapse onto that one crossing (the corpus carries only EQ, EQ3..EQ12, all oracle "when the <Nth> counter is put" gates; LT/LE/NE are unmeasured and keep a plain post-event comparison) -- a plain Forge batch-amount comparison is not modelled. **AttackerBlocked** queues one trigger instance per matching blocked attacker through `checkAttackerBlockedTriggers` (the ordinary per-trigger face scan cannot express per-attacker instances) and carries no Secondary$/Panharmonicon interaction (measured: none in scope) and no granted-AddTrigger$ walk -- the hook scans printed faces only, so an `AddTrigger$`-granted AttackerBlocked trigger never fires (measured: 0 of the 198 corpus files carrying `Mode$ AttackerBlocked` grant the mode through `AddTrigger$`; the granted walks the ordinary scan runs -- checkGrantedWard/Dethrone/StaticTriggers -- are skipped by the hook). | `rules/trigger_match.go` (`attackersDeclaredOneTargetMatches`, `cycledMatches`, `counterAddedMatches`, `attackerBlockedCandidates`, `checkAttackerBlockedTriggers`, `presentConditionHoldsAs`'s PresentZone$), `rules/trigger_eligibility.go`, `rules/trigger_referents.go`, `effects/filter.go` (`blockingTriggeredAttacker`), `effects/context.go` (`TriggeredAttackers`) | M4 (per-declare-step batch latching, layer-granted cycling provenance, the batch-amount CounterAdded shape) |

| **`Phase$ First Strike Damage` (Forge's `COMBAT_FIRST_STRIKE_DAMAGE`) maps to the engine's single combat-damage step** (`state/phase.go`'s `forgePhaseNames`, the one shared phase-name parser): gorge has no separate first-strike step, and this is how Forge's own turn degrades when no creature has first strike. The residual divergence runs the other way: when a first striker IS present, Forge fires the trigger in its own early step and the regular damage later, while this build fires it once, at the single damage step. Measured population: **0** raw `.cards/cardsfolder` lines carry it (the mapping is dormant groundwork), and gorge never models two damage steps in one combat | `state/phase.go` (`forgePhaseNames`) | M4 (a two-step combat-damage turn, or the mapping gated on first-strike presence) |

| **Planeswalker loyalty is now real but NARROW (pw1)**: a walker enters with its printed starting loyalty (CR 306.5b, granted inside `events.Move` so every battlefield entry is covered), and spell/ability damage is ONE real `Damage` event whose CR 306.8/120.3c conversion (loyalty counters, not marked damage; a creature-walker takes both, CR 120.3e) folds into `events.Apply`'s `Damage` case -- so protection (CR 702.16d), prevention replacements and `DamageDone` triggers now see walker damage exactly like every other damage (previously the exchange was a bare `CounterChange` no consumer could see), the zero-loyalty SBA exists (CR 704.5i), `AddCounter<N/LOYALTY>` is a free cost component and the walker's loyalty abilities are once-per-PERMANENT per turn plus own-turn/sorcery-speed (CR 606.3 -- the gate is per object stint, CR 400.7, not per ability index: after [+2] the [0] draw-three is withheld too, and a walker that leaves and returns may activate again; the rv2b fix), and the UI renders a loyalty chip. `S:Mode$ NumLoyaltyAct` statics raise the per-permanent limit (Oath of Teferi's `Twice$ True`/`Planeswalker.YouCtrl` and Urza, Lord Protector's self-scoped `Twice$ True`); the Effect-delivered `NumLoyaltyAct` grants -- The Chain Veil `Additional$ 1`, Comet Stellar Pup `Additional$ 2`, Kaito Dancing Shadow `Twice$ True`, Urza, Assembles the Titans `Twice$ True` (6 corpus files carry the mode; 2 are plain `S:` statics, 4 ride `Effect` SAs) -- are still inert because `Effect` registration covers only `CantTarget`/`CantRegenerate`. Still open: combat damage to a walker (defenders are players only), `[-X]` dynamic costs (20 raw `SubCounter<X/LOYALTY>` lines -- they keep the one-generic fallback and are only caught by the `Planeswalker$` param for the GATING, never costed), walker TOKENS (TokenCreate does not route through `Move`, so a walker token enters at 0 loyalty and the SBA then kills it -- no corpus walker token is reachable today), transform walkers (Jace Vryns Prodigy), prevention/replacement vs walker damage (they key on `Damage` events and do not see the exchange), and a face whose starting loyalty is unreadable (`Loyalty:X`, 2 corpus files -- Nissa, Steward of Elements enters with no loyalty counters and is EXEMPT from the zero-loyalty SBA rather than swept). The 8 non-loyalty `AddCounter` tokens (Devoted Druid's M1M1 untap, Wall of Roots' M0M1 mana ability, two `UnlessCost$` SVars) keep the pre-loyalty one-generic fallback. Counted with GNU `/usr/bin/grep` over `.cards/cardsfolder`: 348 walker files, 348 `Loyalty:` lines (410 `AddCounter<N/LOYALTY>` / 568 `SubCounter<N/LOYALTY>` ability lines), 6 `Loyalty:0`, 2 `Loyalty:X` | `events/apply.go` (`Move`, `Damage` conversion), `effects/damage.go` (`emitObjectDamage`), `rules/sba.go` (`planeswalkerZeroLoyalty`), `rules/mana.go` (`ParseCost`, `addCounterCost`), `rules/legal.go` (`isLoyaltyAbility`, `loyaltyActivationsThisTurn`, `loyaltyAbilityLimit`), `rules/cast.go` (ability-branch AddCounter settle) | M4 (walker combat damage, [-X] cost grammar, walker tokens, transform walkers, prevention vs walker damage, Effect-delivered NumLoyaltyAct) |
| The damage pipeline's DamageSource$/ValidPlayers$ machinery (rv2b) has measured remainders. **DamageSource$ specs `Defined` cannot model keep the resolving source** (today's pre-fix behaviour, never a target-redirect): `Imprinted` x4, `Any` x1, the four `Valid <card-spec>` forms, `Spawner>TriggeredCardLKICopy` x1, `ChosenCard` x1 -- 11 of the 205 raw `.cards/cardsfolder` `DamageSource$` lines (199 files); the other 194 resolve. **DamageAll `ValidPlayers$`** resolves the 140-line Player/Opponent/You predicate family plus the selector forms (Targeted, Remembered, TargetedController) and `OppNonTriggeredTarget` x9; 4 exotic lines fail closed to no player damage (`FlippedTails` x1, `Player.controlsCreature.White_GE1` x1, `Player.Opponent+wasDealtDamageThisGameBy Self` x1, `Opponent.IsRemembered` x1) -- of 427 raw DamageAll lines (402 files), 161 carry `ValidPlayers$`. **The `<Ref>$<Property>` count family** (Targeted/ParentTargeted/ThisTargetedCard over `Ctx.Targets`, the TriggeredCard/TriggeredAttacker family and Remembered over `Ctx.Remembered`; props CardPower/CardToughness/CardManaCost/CardCounters.<KIND>/Valid/Converge with /Op) is implemented in `evalRefProperty` (Converge reads the pay-time FlagConverged CastInfo stamp, prefering the fire-time `TriggerContext.TriggerConverge` snapshot so a spell countered between push and resolution still reads the colours actually spent; the capture gate stamps also when a battlefield reader's SVars name `TriggeredCard$Converge` -- Magmablood Archaic's Y); the exotic heads still evaluate to zero (measured over raw `SVar:` bodies): `TriggeredCard$CastTotalManaSpent` 21 (the PLAIN `Count$CastTotalManaSpent` head is real since castprov1 — the pay-time FlagManaSpent Amount folded into `Object.ManaSpent`; only these ref-property readers of another cast's spend stay), `TriggeredTarget$LifeTotal` 13, `TriggeredCard$CardCounters.ALL/AGE` 16, `TriggeredCard$CardNumColors` 7 + `Targeted$CardNumColors` 3, `TriggeredTarget$CardsInHand` 4, `Remembered$ChromaSource`-family 5, plus ~20 one-off heads. DamageDone executes resolving through `Defined$ TriggeredSourceController`/`TriggeredTargetController` (Flameblade Angel, Harsh Justice, Greatbow Doyen) read the pg2 trigger roles | `effects/damage.go` (`newDamageRider`, `validPlayers`), `effects/context.go` (`definedSpec`), `effects/count.go` (`evalRefProperty`) | M4 (the 11 DamageSource$ lines, the 4 ValidPlayers$ lines, the exotic count heads) |
| **The starting player is uniformly random but the toss winner never CHOOSES (CR 103.1)**: `rules.New` draws the starting seat from the engine rng (one `IntN` over the seats, drawn before any per-seat shuffle; when the genesis deal itself eliminated a seat the pre-drawn value -- uniform over every seat -- is kept as the first candidate and rejection-sampled until it names a survivor, so the determination stays uniform over the SURVIVORS and the no-elimination stream stays exactly one `IntN`) and announces it with one `Note` ("X won the toss") BEFORE the first shuffle and opening deal -- CR 103.1 precedes 103.2-103.4, matching Forge's GameAction and manabrew's game loop. After the deal, the resolved starter is authoritative `state.Game.StartingPlayer` (folded at genesis through `events.StartingPlayerChange` without appending an event -- genesis is replayed from Config, including its seeded toss, so recorded matches still replay), which is what the `Count$StartingPlayer` count head (Desert Cenote and 12 more corpus files) and the view's pregame projection read; an opening-hand effect that accepts `BecomeStartingPlayer$` (Impatient Iguana, rules/opening_hand.go's post-mulligan opening round) emits the same event kind, so the recorded designation follows it. A terminal genesis still emits no second Note and leaves the designation absent (`HasStartingPlayer` false). The Note text carries the deck identity, never the display PlayerName (F3 keeps PlayerName out of the event chain); `view/describe.go` renders that one Note through `player()`, so a seated human reads their own name. The keep/mulligan prompt names who plays first (`<starter> plays first. ...`), so the decision is made knowing play/draw without opening the hidden transcript. What is missing is 103.1's second half: the toss winner may choose who takes the first turn, and this build gives them no such decision -- a start-of-game choice decision (new `decision.Kind`, a bot-policy arm, and a host conveyance) is deliberately out of scope here. Every playable game silently gives the toss winner the first turn | `rules/engine.go` (`New`), `rules/mulligan.go` (`PregameStarter`, `keepMulliganPrompt`), `rules/opening_hand.go` (`handleOpening`), `effects/count.go` (`Count$StartingPlayer`), `view/describe.go` | M4-or-later (a start-of-game choice decision: toss-winner-chooses) |
| A mulligan declaration's REDRAW (the seven `MoveZone`s, the `Shuffle` and the seven draws) resolves immediately after that seat's declaration, interleaved with the pass's remaining keep/mulligan asks, rather than after every un-kept seat in the pass has declared: CR 103.4/103.5 has all mulligans happen simultaneously. The DECISION order is the CR 103.5 pass order (every un-kept seat declares in turn order from the starting player, then the pass repeats — `TestMulliganDeclarationsPassRoundTheTable`, 2- and 3-seat), and per-seat observable state is identical; only the private redraw events' interleaving with the pass's remaining asks differs. (The decision-order fix itself is what moved the 4/6/8-seat heads in the rv2a starting-player merge onto advanced main — measured first divergences: 4 seats event 53 `decision_ask` mulligan player 2 vs main's player 1, 6 seats event 69 player 4 vs 3, 8 seats event 85 player 2 vs 1; the 2-seat head did not move.) | `rules/mulligan.go` (`handleMulligan`) | M4-or-later (redraws resolved together after each pass's declarations) |
| **The starting player is uniformly random but the toss winner never CHOOSES (CR 103.1)**: `rules.New` draws the starting seat from the engine rng (one `IntN` over the seats, drawn before any per-seat shuffle; when the genesis deal itself eliminated a seat the pre-drawn value -- uniform over every seat -- is kept as the first candidate and rejection-sampled until it names a survivor, so the determination stays uniform over the SURVIVORS and the no-elimination stream stays exactly one `IntN`), records the draw with one `Note` ("X won the toss and takes the first turn" in a playable game, or the truthful "X won the toss; the game ended before the first turn" when an undersized opening deck ends genesis; both carry the deck identity, never the display PlayerName -- F3 keeps PlayerName out of the event chain), and rotates the CR 103.5 mulligan round to `AliveFrom(start)`. What is missing is 103.1's second half: the toss winner may choose who takes the first turn, and this build gives them no such decision -- a start-of-game choice decision (new `decision.Kind`, a bot-policy arm, and a host conveyance) is deliberately out of scope here. Every game with a configured seat records the toss, including a terminal opening deal with no survivor; the winner-chooses stand-in's population is every playable game, and the choice is silently dropped (a human who wins the toss on the demo gets the first turn whether or not they wanted it) | `rules/engine.go` (`New`) | M4-or-later (a start-of-game choice decision: toss-winner-chooses) |
| **`K:Affinity:Affinity` discounts nothing (1 card, Sojourner's Enforcermite)**: the affinity expansion (cards/keywords.go's `case "Affinity"`, a self-scoped ReduceCost static priced by `Count$Valid <spec>.YouCtrl`) evaluates its spec through the ordinary filter vocabulary, and "Affinity" is not a type word there, so the count fails closed to 0 -- the spell always costs full price, deterministically and conservatively. Measured 1 raw line over the corpus. The other two exotics the Affinity task named are NOT stand-ins: `K:Affinity:Permanent.token` (Junk Winder) counts real tokens through `Count$Valid Permanent.token+YouCtrl` (pinned by `TestJunkWinderAffinityCountsTokensOut`), and the 75 plain type/subtype specs work through the existing filter. Note the joining separator the expansion emits is load-bearing: a dot-less spec must join the controller qualifier with `.` (a `+` join reads the whole string as one base type word and fails closed), while a spec that already carries a dot must join with `+` (a second dot glues `YouCtrl` onto the previous predicate token, also unknown) | `cards/keywords.go` (`case "Affinity"`), `effects/filter.go` (`MatchesObjectCtx`'s base/predicate split) | M4 (only if "Affinity" ever becomes a meaningful countable type, which no CR rule suggests) |
| The Equip expansion's trailing colon fields are read (eqcm1: `ReduceCost$`/`ActivationLimit$` riders ride the minted SA verbatim; the first space-free trailing field is the target-restriction spec passed through as `ValidTgts$`, pinned by `rules/equip_cost_modifiers_test.go`), with these measured remainders: the two `Targeted$`-referenced equip reductions -- `belt_of_giant_strength` (`SVar:X:Targeted$CardPower`) and `dragonfire_blade` (`SVar:X:Targeted$CardNumColors`) -- still resolve to **0** (no discount, conservative) because `ownReduceCost`'s Ctx carries no target bindings, so the reduction would need a target-anchored evaluation; `blackblade_reforged`'s restriction `Creature.YouCtrl+Worthy` fails closed (unknown predicate), so its equip is never offered at all (narrower than the pre-eqcm1 unrestricted offer, the documented fail-closed direction); and the **3** raw `AlternateCost$` rider fields (`K:Equip:...:::AlternateCost$ ...`: bloodthorn_flail, transmogrants_crown, gavel_of_the_righteous) are still dropped from the minted SA -- the rules side reads no `AlternateCost` param on an activation, so passing them through would be inert; the space-free-vs-prose split that picks the restriction spec is measured over all 646 raw `K:Equip` lines (every spec space-free, every description space-bearing, the one-word descs only ever trailing a real spec) | `cards/keywords.go` (`case "Equip"`), `rules/legal.go` (`ownReduceCost`) | M4 (target-anchored ReduceCost; the `Worthy` predicate; activation AlternateCost) |
| **`Manifest` is real but the face-up mechanic (CR 708.6) is not implemented — a manifested card stays face down forever in this build.** The primitive exists (`effects.Register("Manifest", effManifest)`, `effects/zone.go`): the top card of each `searchPlayers`-named player's library moves library→battlefield as the REAL card object (never a token mint) via one Secret `MoveZone` carrying `Counter "entered_face_down"` (events/apply.go folds the state `FaceDown`, and Move's entry grants skip loyalty/lore for a face-down entry); derived characteristics are a vanilla 2/2 Creature (CR 708.5 — even a manifested land, pinned), with other permanents' layer effects still applying on top — including layer-4 type grants and strips, which since the facedown-layer4 fix run over the CR 708.5 base set (`typeCharacteristics` selects `FaceDownTypeWords()` as the walk's base instead of returning it before the walk, so a Maskwood Nexus grants every creature type to a manifested 2/2 exactly as to a face-up creature, while a compiled `Affected$` predicate is answered by the `ExtraTypes`-aware text oracle because `matchesWithTypes` clears the printed-face-only compiled sidecar); printed abilities/triggers/statics are suppressed while face down by the one shared gate `rules.faceDownPrintedHides` (CR 708.8) in the offer loop, the mana-ability collector, the trigger scan and both static scans; leaving the battlefield clears `FaceDown` (CR 708.9's reveal, pinned) and the view redacts the card to everyone but its controller. **Unimplemented for Manifest, loud:** `ManifestDread` (a DIFFERENT API, 31 corpus files), `RememberManifested$ True` (7 lines), `Defined$`-object manifests (9 lines), the `Choices$`/choice-asking forms (2) and unresolvable `Amount$` bodies (Y, bare X outside a cast) — each emits the SAME `"unimplemented API Manifest"` note the unimplemented fallback always emitted and moves nothing. Measured population of plain Manifest: **32 files / 33 raw lines** (`DB$ Manifest\b` 23 files/24 lines + `SP$/AB$ Manifest |` 9 files/9 lines — the brief-reported "54 files" double-counted `ManifestDread`). **Cloak (`api:Cloak`, 11 corpus files / 11 raw lines, all `DB$`) is REAL on top of the same machinery** (cloak1): the same Secret MoveZone shape with Counter `"entered_cloaked"` folds BOTH `FaceDown` and the new `state.Object.Cloaked`; the ward {2} is part of the cloak status itself, not a printed or granted ability — `derivedWith` appends `"Ward:2"` to the derived keyword list for a cloaked object, and the face-down trigger gate's one exception runs `checkGrantedWardTriggers` for a Cloaked object (with the `printed` suppression set empty while face down, so a real face that prints Ward cannot suppress the cloak-ward) — so targeting a cloaked 2/2 meets the real pay-or-counter ask through the existing `beginWardPayment` machinery. The new `faceDown` filter predicate (effects/filter.go) makes Veiled Ascension's `Creature.faceDown+YouCtrl` specs resolve. Implemented shapes: the per-player top-card cloak (`DefinedPlayer$`, or `Defined$ TopOfLibrary`), the ctx-Remembered object cloak (become_anonymous, hide_in_plain_sight, expose_the_culprit), literal/SVar `Amount$`, and the riders `Tapped$`/`Shuffle$`/`RememberCloaked$`/`SubAbility$`; the `Choices$` cloak-from-hand chooser (vannifar), `Defined$ ValidLibrary` (etrata) and unresolvable bodies stay LOUD (`"unimplemented API Cloak"`). Measured at the cloak1 gate: `make report` moved 26997 (80.2%) → 27003 (+6); no carrier is in any repo deck, so the heads are unmoved (verified). Pinned by `TestVeiledAscensionUpkeepCloaksFaceDown`, `TestVeiledAscensionFlyingCountersFaceDownCreatures`, `TestCloakedWardDeclinedCountersTheSpell`, `TestCloakedWardPaidLetsTheSpellResolve`, `TestManifestedCardGetsNoWard`, `TestCloakedLeavesBattlefieldReveals`, `TestCloakOutOfScopeShapesStayLoud`, `TestUnexplainedAbsenceCloaksEachControllerTop`. Ordinary target filters read the printed face (the documented layer-4 filter limitation), so an "Elf" filter cannot see a manifested Elf — and a creature-targeting spell cannot see a cloaked card whose printed face is not a creature (the ward is only reachable on creature-faced cloaks); pinned by `TestRealityShiftManifestsFaceDown`, `TestManifestedLandIsA22Creature`, `TestWhisperwoodManifestsOnEndStep`, `TestSultaiEmissaryDeathTriggerManifests`, `TestManifestedCardRevealsWhenItLeaves`, `TestManifestOutOfScopeShapesStayLoud` | `effects/zone.go` (`effManifest`, `effCloak`), `events/apply.go` (MoveZone decode), `rules/layers.go` (`typeCharacteristics`, `derivedScalarFrom`, `faceDownPrintedHides`, the derived cloak-ward append), `rules/trigger_match.go` (the cloaked granted-ward exception), `effects/filter.go` (`faceDown`), `rules/legal.go`, `rules/statics.go`, `rules/mana_activation.go` | M4 (turn-face-up CR 708.6 for BOTH manifested and cloaked cards — the Morph/Megamorph/Disguise ticket owns the shared path; ManifestDread; RememberManifested$) |
| **Token-creation replacement (`repl:CreateToken` / `DB$ ReplaceToken`) is implemented but NARROW (tokrepl1)**: the mandatory core works — `replacementEvent` maps `events.TokenCreate` to `CreateToken`, the matcher's case reads `ValidToken$` (against a would-be-token shallow snapshot built off `g.Tokens[ev.Text]`, You-side predicates relative to the replacement source's controller; unknown token key fails closed), `ValidPlayer$` and the shared condition gates, and the dispatch reads each body's `Type$` directly in rules (the replaceDamageAmount precedent): `ReplaceToken` (a CSV rewrite: Academy Manufactor's one Clue → Clue+Food+Treasure, Divine Visitation's creature token → the real 4/4 white Angel), `Amount` (default double; `Thrice`; `HalfDown` floor via the shared `replCountOp` word ops) and `AddToken` (Xorn's extra Treasure). Composition is deterministic scan order with each match's `ValidToken$` re-checked PER PLAN MINT (DV after a doubler replaces each doubled mint that is still a creature token), and the finished plan is emitted through the direct `events.Emit`+`observe`+`checkTriggers` path so no mint ever re-matches (a doubler cannot loop on its own output; two doublers give exactly 4, pinned) — a path that, like the `composeUpdatedReplacements` precedent it copies, skips `Engine.emit`'s post-apply progress block, so a replaced token creation clears neither the declined-Delve cast suppression (`suppressedCast`/`castAborts`) nor a "for as long as" control effect (`expireControl`); both wait for the next state-changing event, deterministic and replay-safe. **The stand-ins**: an `Optional$ True` R: line takes the deterministic decline (event verbatim, no ask — Flitwing Lyev, Jinnie Fay, and the three `Layer$ Copy` Esix/Moonlit Meditation/Mirrormind Crown cards whose `TokenScript$ Chosen` bodies would be a mid-replacement ask anyway); a `TokenScript$ Chosen`/`ValidChoices$` body and a `Type$ ReplaceController` body each emit ONE loud Note naming the unimplemented shape and leave the event verbatim (crafty_cutpurse is the one ReplaceController line; its Effect-granted R: line additionally never matches today because its `ActiveZones$ Command` fails the shared active-zone gate while the granting effect is active on a battlefield creature — a pre-existing Effect-granted-ActiveZones quirk, unmeasured beyond this one card); an `Amount$` that is neither a literal nor a `replCountOp` word op is a loud Note + verbatim (measured corpus population: none — the 35 carrier files carry only `1`/`Twice`-default/`Thrice`/`HalfDown`); a halving composed AFTER a multiplier applies per mint rather than to the plan total (corpus-unreachable composition); there is NO CR 616.1 order choice over competing CreateToken replacements (scan order instead; non-commuting compositions are reachable in Commander but no repo deck carries any of the 35, so no golden game exercises one); and `effToken`'s `want := g.NextID` rider bookkeeping lands on the plan's FIRST mint only — replacement EXTRA mints get no Remember/Tap/Attach/P-T-set/Imprint riders (Academy Manufactor remembers only the first of its three, Doubling Season's doubles enter no `RememberTokens$` list). Chatterfang's `AddToken` script carries no `Amount$`, so it adds ONE squirrel per token event where the oracle text says "that many" — Forge's own script under-specifies and this build follows the script. `EffectOnly$ True` (Doubling Season's family) is read and held: today every TokenCreate emitter is effect resolution, so the gate is vacuously satisfiable — a cost-created-token provenance marker, when one lands, must be read here. Measured population (GNU `/usr/bin/grep`): 35 card files carry `R:Event$ CreateToken` (32) or `DB$ ReplaceToken` (35). Pinned by `TestDivineVisitationReplacesCreatureTokens`, `TestDoublingSeasonDoublesTokens`, `TestAcademyManufactorClueFoodTreasure`, `TestXornAddsTreasure`, `TestTokenReplacementsComposeScanOrder`, `TestTokenReplacementExtrasNeverRematch`, `TestTokenReplacementOptionalDeclines` | `rules/replacement.go` (`replacementEvent`, `replacementMatchesRemembered`'s CreateToken case, `tokenSnapshot`, `continueCreateTokenReplacements`, `applyTokenReplacementToPlan`, `tokenReplaceCount`) | M4 (asks for the Optional$/ValidChoices bodies, ReplaceController, rider propagation to extras, the CR 616.1 order choice, cost-token provenance) |
| A Replicate (CR 702.55a) copy keeps the original's target: the kw:Replicate expansion's copy trigger carries `MayChooseTarget$ True` (the Storm shape), and `MayChooseTarget$` IS read — but only to gate the per-copy "copy keeps its targets" Note (`effects/copy.go` `effCopySpellAbility`'s MayChooseTarget arm), not to offer a chooser — so "You may choose new targets for the copies" (the oracle wording on 18 of the 21 carriers) is a stand-in: each replicate copy inherits its target, exactly like every other copy family (the general copy row above; Changing Loyalty's copy attaches to the same creature, which is the desirable outcome here). Also conservative: the replicate count ask's max is priced with `castable` over the RAW composed cost, so a ReduceCost-style modifier that would make one more payment payable is not seen (under-offers, never over-offers), and the bound is pool-only at ask time — a Convoke/Harmonize contribution announced AFTER the count can never extend it (the count ask precedes the convoke ask, the same CR 601.2b reading); when the bound collapses to 0 under the proposal, the explicitly chosen "(replicated)" mode degrades to a plain cast with a loud Note (`replicateAsk`'s max==0 arm), never a silent downgrade. The bound's composed cost is priced through `nonManaCastable`, whose TapPermanent parts reserve candidates against each other and whose fixed PayEnergy parts sum (the checker the payment path faces), so a repeated tap/energy replicate cost offers exactly what the board can pay | `effects/copy.go` (`effCopySpellAbility`'s MayChooseTarget arm), `rules/cast.go` (`replicateAsk`, `nonManaCastable`) | M4 (the copy-target task; the modifier-aware count bound; a convoke-extended count bound) |
| **Multikicker (CR 702.43) is real but its bot policy is the decline stand-in (multikicker1)**: the "multikicked" cast option (rules/legal.go, beside the replicate offer), the count ask (`multikickAsk`, the replicateAsk shape -- CR 601.2b's count announcement before Convoke/X, max bounded by the `castable` walk, 0 = the loud-Note plain-cast degrade), and the trailing pay-time `FlagMultikicked` CastInfo routing the count into `state.Object.TimesKicked` (events.Apply's CastInfo case; the stack->battlefield move preserves it, both leave-battlefield resets clear it) are all real, with the `Count$TimesKicked` head (the ReplicatePaid provenance read; a COPY was never kicked and reads 0) and the `/Thrice` op in `applyCountOp`. The {X} competition is handled: the count rides its OWN trailing event, so Comet Storm's {X} and its kick count never share an Amount, and the spell's OWN target ask reads the pending count seeded by `targetBoundCtx` into `Ctx.TimesKicked` (TargetMin/Max$ TargetsNum = kicks+1) because the CastInfo has not run at announcement time. Measured corpus: 19 files carry `K:Multikicker`, 30 carry `Count$TimesKicked` (11 of them legacy plain-Kicker carriers, including the two-part Kicker family whose kickedboth cast counts 2); `make report` moved 26126 (77.6%) -> 26143 (77.7%) (+17 -- Enclave Elite and Quag Vampires stay blocked on the unrelated kw:Landwalk gap), and no carrier is in any repo deck so the heads are safe by construction and verified unmoved. Two deliberate narrows: a bot answers the count ask through botpolicy's KChoose `default` arm -- first offer, option 0 = "No multikick", the deterministic decline (the replicate precedent; no dedicated arm) -- so a bot never pays a kick; and a plain-Kicker cast mode's count rides the trailing event ONLY when the face carries a `Count$TimesKicked` SVar (`faceWantsTimesKicked`) -- the gate is the design (unrelated kicked casts stay byte-identical), so a future plain-kicker carrier that reads the count without the SVar stays 0. Pinned by `TestMarshalsAnthemMultikickedETBReturnsKickedCount`, `TestMarshalsAnthemPlainCastETBAsksForNothing`, `TestEverflowingChaliceKickedEntersWithChargeCounters`, `TestCometStormXAndMultikickRideSeparateEvents`, `TestMultikickAskBoundedByPool`, `TestStrongholdArenaTimesKickedThrice`, `TestIntoTheRoilKickedEmitsNoMultikickEvent`, `TestMultikickBotDeclinesThroughTheFirstOfferArm` | `rules/legal.go` (the multikicked offer), `rules/cast.go` (`multikickerCost`, `multikickAsk`, `modeFlags`, `payCast`'s trailing event, `faceWantsTimesKicked`), `rules/stack.go` (`targetBoundCtx`'s pending-count seed), `effects/count.go` (`TimesKicked`, Thrice), `effects/registry.go` (`Ctx.TimesKicked`), `events/apply.go` (CastInfo routing + resets), `state/object.go` (`FlagMultikicked`, `TimesKicked`) | M4 (a bot-quality multikick policy) |
| **Foretell (CR 702.126) is real but three adjacent shapes stay unimplemented (ft1)**: the {2} face-down hand-exile special action (the `foretell` cast mode in `legalActionsPriced`'s hand walk, beside the Suspend offer, gated on "during your turn" only — deliberately no instant/sorcery-speed check, though the hand walk's own castRestricted/castSuppressed/spellTimingOK continues bound it the same way the Suspend offer is), the later foretell-cost cast from exile (`foretell_cast`, offered in the exile walk BEFORE the warp gate's continue at `ParseCost(f.KeywordParam("Foretell"))` — the colon parameter prices the LATER cast, never the {2} action; a param-less face falls back to the rule's action default {2}, latent, 55/55 carriers carry one), the log-derived "later turn" gate (`foretellCastAvailable` in `rules/altcast.go`, the warpRecastAvailable shape: latest MoveZone of the id into exile, a flag-bearing CastInfo before it with no intervening move, at least one TurnChange after — the flag alone cannot say it), the `FlagForetold` provenance both markers carry (the action's own CastInfo in `payCast`'s foretell branch, and `modeFlags("foretell_cast")` — the stack→battlefield persistence is what lets an ETB reader see it), the `Count$Foretold.<ifTrue>.<ifFalse>` branch head in `evalCountBody`'s tail (operands through `evalCountOperand`, so a literal or an SVar name resolves — Starnheim Unleashed's `Count$Foretold.X.1` reads the paid X; a missing branch fails closed `(0,false)`), bare `Condition$ Foretold` through the shared bare-condition gate (`effects/conditions.go`, the Kicked precedent — both corpus carriers, Poison the Cup and rebalanced Alrund's Epiphany, are exactly that shape), and the note machinery the ETB reader needs: a new `events.NoteNumber` Kind (Apply folds it into `Object.NotedNumber`, reset with the X/CastFlags window when the permanent leaves the battlefield), `DB$ Pump | NoteNumber$ X` read in `effPump` (terminal note, never a pump — the corpus's one carrier is Lupine Harbingers' exile trigger noting `Count$YourTurns`), the `Count$NotedNumber` reader, the `kw:etbCounter` expansion's condition-gate third field passed through to the shared `replacementConditionHolds` — split on ` | ` into `CheckSVar$` + optional `SVarCompare$` params (round 2 stuffed the whole field into `CheckSVar` and the three `|`-bearing carriers' gates failed closed, silently un-applying their counters; those two params plus a gate field's own `ValidCard$` when its spec still constrains Self (castprov1: epochrasite's `Card.Self+!wasCastFromYourHandByYou` and the 10 escape-counter carriers' `Card.Self+escaped` now reach the matcher; a Self-less fragment would widen the default `Card.Self`'s match, the drop's original reason — measured, all 11 raw carriers are Self-constrained, none in any repo deck, heads unmoved), and the `Count$wasCastFromYourHandByYou.<ifTrue>.<ifFalse>` branch head the gates read (an ordinary hand-origin cast carries no CastFlags bit, so the provenance is the object's latest `PutOnStack` via `Host.WasCastFromHandByYou`'s log scan, replay-derivable; the Myojin cycle's 12 `FromHand` carriers are the population). Measured: 475 raw `K:etbCounter` lines over 475 files, 65 carrying a gate field over 65 files (WasKicked 29, FromHand 12, RaidTest 6, X 5, Morbid 4, WasForetold/Revolt/Mod/Ferocious/inline-Count$ one each, plus the 3 `|`-bearing SVarCompare pairs); the Moved case had previously ended `return true`, so the shared read makes these condition gates BIND — blast radius beyond Lupine is Protean Raider's `CheckSVar$ RaidTest` and Archelos, Lagoon Mystic's two `IsPresent$ Card.Self+tapped/untapped` lines, both directional fixes, none in a repo deck), and `zoneGate`'s admission of a card's own hand-origin move (the no-`TriggerZones$` ChangesZone self-triggers whose destination is not the battlefield — the same courtesy the Discarded/Cycled admissions extend; measured population 14 blocks / 14 files: the 10 `Origin$ Any → Graveyard` "from anywhere" elders (Emrakul/Kozilek/Ulamog/Dread/Guile/Vigor/Purity/Hostility/Serra Avatar/Worldspine Wurm — discard-shuffle/exile fires, oracle-correct), bone_rattler (from-anywhere exile-and-conjure), gixian_recycler (explicit `Hand,Library`), fear_of_change (no `Origin$` — its "enters or dies" graveyard arm now fires on a hand discard too, the Forge-faithful no-zone-restriction reading, unlike the oracle) and Lupine Harbingers). Measured corpus: 55 `K:Foretell` lines over 55 files, 3 `Count$Foretold`. **Remaining, own tickets:** `Foretold$ True` as a ChangeZone param (2 files: ethereal_valkyrie, the_foretold_soldier — a trigger marks an arbitrary exiled card foretold with a dynamic cost), the `Card.foretold` object predicate (4 files: alrund, singing_towers_of_darillium, niko_defies_destiny + a rebalanced copy), and dynamic foretell-cost reduction (Lorwyn's foretell statics) — unscoped. The type choice Haunting Voyage's resolution reads is a REAL mid-resolution `KChoose` ask since ct1 (the class-wide ChooseType-at-resolution ticket, closed by the row below). No repo deck plays a foretell card, so the heads are safe by construction (verified unmoved). Pinned by `TestForetellActionOfferedOnYourTurnAtTwoGeneric`, `TestHauntingVoyageForetellUnlocksTheReturnAllHalf`, `TestHauntingVoyageOrdinaryCastReturnsUpToTwo`, `TestStarnheimUnleashedForetoldCreatesXTokens`, `TestStarnheimUnleashedPlainCastCreatesOne`, `TestLupineHarbingersWasForetoldEntersWithNotedTurns`, `TestForetellOrdinaryExiledCardNeverGetsTheCastOffer`, the eval-level `TestForetoldHead*`/`TestNotedNumberHead*`/`TestBareConditionForetoldGatesOnTheCastFlag`, the etbCounter gate pins `TestMyojinCastFromHandEntersWithDivinity`, `TestMyojinNotCastFromHandEntersWithoutDivinity`, `TestHotheadedGiantNoRedCastEntersWithCounters`, `TestFreestriderCommandoUncastEntersWithCounters`, `TestSteelExemplarUncastEntersWithCounters` and the elder fire `TestEmrakulDiscardedFromHandShufflesTheGraveyardBack` | `rules/legal.go` (the two offers), `rules/cast.go` (beginCast's two modes, continueCast's early return, modeFlags, payCast's branch), `rules/altcast.go` (`foretellCastAvailable`), `rules/trigger_match.go` (`zoneGate`'s self hand-origin-move admission), `rules/replacement.go` (the Moved case's shared condition read), `rules/trigger_eligibility.go` (NoteNumber's interest 0), `effects/count.go` (`Foretold.`, `NotedNumber`, `wasCastFromYourHandByYou`), `effects/conditions.go` (bare Condition$ Foretold), `effects/combatfx.go` (`effPump`'s NoteNumber arm), `cards/keywords.go` (the etbCounter gate split), `effects/registry.go` + `rules/stack.go` (`WasCastFromHandByYou` Host method), `events/event.go` (`NoteNumber` Kind, the `foretold` flag name), `events/apply.go` (NoteNumber Apply + the two resets), `state/object.go` (`FlagForetold`, `NotedNumber`) | M4 (the three adjacent shapes; the cast-provenance cell is CLOSED by castprov1 and castprov2 — the `Card.wasCastFromYourHandByYou` predicate splits at the rules-side match sites, `replacementCheckValue` evaluates a face's real `SVar:X` body before the announced-X fallback, `Count$CastTotalManaSpent` is the pay-time FlagManaSpent capture, the bare `!CastSaSource` count qualifier is the exclusion-of-own-cast read, castprov2 added the `ConditionDefined$ TriggeredCard` gate group with the per-member `wasCastFromYourHandByYou` present-spec strip (Amped Raptor; a DB$ Play over a remembered population now excludes the trigger's captured entry — a deliberate, disclosed read, not a silent ride-along: Forge's triggering objects are a channel of their own, never members of the shared remembered list, so the Play must read the resolution chain's own memory; measured, ZERO corpus files combine a trigger `RememberObjects$` capture with `DB$ Play | Defined$ Remembered`, so no carrier loses a legitimate candidate), the argumented `!CastSaSource$<Property>` aggregate (call_forth_the_tempest's `$CardManaCost`; `/Plus.N` resolves through the generic /Op peel + the bare exclusion, thunder_salvo), the trigger-side bare `!CastSaSource` name exclusion + the `ActivatorThisTurnCastEach$` per-alternative first-cast read (Alania; the round-2 review fix: the Each tally is evaluated ONLY over alternatives the CURRENT CAST matches — instant→sorcery→second instant does not fire on the sorcery alternative's earlier tally, pinned by `TestAlaniaSecondInstantAfterASorceryDoesNotTrigger`; a Each-without-ValidCard$ shape is unsupported, fail closed), and the bare `wasCastByYou` split at the zoneGate/Moved-replacement sites (Zacama's ETB family, Nine-Lives Familiar's gate field); left fail-closed and ledgered: the 9 stack-grant statics, the 1 AB-target (`cycle_of_life`, also unregistered `ThisTurnCast`) and 2 `Mode$ Countered` wasCastByYou shapes, the 3 Counter-replacement carriers, the bare `wasCastFromYourHand` (no "ByYou") 46-file family, and the LKI-copy defined groups (`TriggeredCardLKICopy` 10, `TriggeredNewCardLKICopy` 3 — re-measured at the round-2 gate; the brief's 11/4 overcounted non-gate uses) |
| **The mid-resolution `SP$ ChooseType` ask is REAL (ct1) but three adjacent shapes stay silent**: a `Type$` category this build cannot enumerate — Basic Land 26 / Card 17 / Land 5 / Planeswalker 2 raw sites plus the one-off CreatureInTargetedDeck / Nonbasic Land / Shared — keeps the loud Note plus a nonsensical CREATURE-type fallback ("choose a basic land type" records "Human"); the sibling primitives `effChooseColor` (records first-WUBRG "W") and `effChooseNumber` (records 0) never ask mid-resolution; and a choice with zero or ONE offerable type takes the deterministic fallback without an ask (the strict-supersets convention — the single legal answer equals the fallback's pick, except in the controller≠owner corner where the owner-based enumeration and the controller-based fallback can disagree, where the fallback still wins). What works: `Type$ Creature` or an absent `Type$` with two or more offerable types poses a real `KChoose` (Min=Max=1, Prompt "Choose a creature type", the cast-time ask's own "type" option shape) to the first `Defined$` player, else the resolving controller, through the shared enumeration `rules/cast.go`'s `creatureTypeOptions` (the same list the cast-time ETB ask builds, so the two can never disagree); the "choosetype" resume arm re-enters through `Ctx.ChosenType` (consumed and cleared, fx42), the re-entry emits exactly one `Choose` event with the answered type, and the downstream `Card.ChosenType`/`IsNotChosenType` filters read it unchanged. A host that cannot ask falls to the unchanged deterministic fallback (R-9, byte-identical, no Note). Measured corpus at `0fc47d35`: 180 raw ChooseType lines (SP 33 / DB 114 / AB 33), 176 compiled carrier cards, 97 with a silent at-resolution site — of which exactly ONE (Raise the Palisade, avengers-assemble Commander) is in any repo deck and none in the legacy golden decks, so the heads are safe by construction. Pinned by `TestChooseTypeAsksAndSuspendsMidResolution`, `TestChooseTypeReEntryEmitsTheAnsweredTypeOnce`, `TestChooseTypeSingleOptionTakesTheFallbackWithoutAsking`, `TestChooseTypeChooserFollowsDefined`, `TestChooseTypeNonCreatureCategoryStaysNotePlusFallback` and `TestHauntingVoyageForetoldReturnsTheChosenTypeNotTheFallback` | `effects/choose.go` (`effChooseType`, `effChooseColor`, `effChooseNumber`), `rules/cast.go` (`creatureTypeOptions`, `TypeChoices`), `rules/resolution.go` (the "choosetype" arm) | M4 (a non-creature option enumeration; the sibling ChooseColor/ChooseNumber asks) |
| The fire-time ability-object role (abcopy1) CLOSED this row's two defects: rules captures the minted ability wrapper's id into `TriggerContext.TriggerAbility` at the moment the `AbilityPush` fires (checkTriggers runs synchronously inside emit, so the wrapper is deterministically the stack top and a replay re-derives the same capture with no event-schema change -- the `TriggerPaidX`/`TriggerConverge` mechanism), and `effects/context.go`'s `TriggeredSpellAbility` case, `refTargets`'s same ref, `effCopySpellAbility` and `effChangeX` all prefer the role when non-zero. Ability-cast `Defined$ TriggeredSpellAbility` COPY triggers now copy the wrapper (it is on the stack, the zone guard passes) and the copy resolves; per-trigger provenance is exact even with SEVERAL activations of one permanent outstanding (each trigger binds its own activation). `changeXAbilityObject`'s topmost-wrapper scan remains ONLY as the role-absent fallback (hand-built/synthetic contexts -- corpus-unreachable: every real ability-cast trigger carries the role). The AB-with-`Cost$` copy carriers (Rings of Brighthearth {2}, Kurkesh {R}, Battlemages' Bracers {1}, Chandra's Regulator {1}, and since abcopy2 the SPELL-CAST arm -- Mirari {3}, Riku of Two Reflections {U}{R}, Cloven Casting {1}, Reflective Golem {2}, Leyline of Resonance {1}, Ulalek {C}{C}, Mica {1}{U/R}) route through the triggered-cost pay/decline window (`rules/stack.go` resolveTop's gate and the "optional" arm in `rules/resolution.go`, armed when the firing trigger's context carries EITHER event role -- the activation `TriggerAbility` or the spell arm's `TriggerCard`, since a SpellCast fires on `PutOnStack` whose Obj IS the spell and no wrapper is minted); an unpriceable cost (`PayLife<X>`, Verrak; `Sac<1/Artifact>`, Mica; `SubCounter<2/P1P1>`, Verazol; `tapXType<3/Creature>`, Aziza; the `Mandatory Sac<1/CARDNAME>` parse, Ominous Lockbox; mizzix_replica_rider's `{1}{U/R}` -- `Cost.Priceable` rejects every Hybrid pip, so it lands decline-only) poses the ask with decline only -- the ParseUnlessCost hard-decline convention, never a free copy. A context-less synthetic push (no role) keeps the free-executor semantics. The CAST arm is exact: `PutOnStack`'s event carries the spell object itself. abcopy2 also taught `effCopySpellAbility`'s target-derived arm the `Defined$ ValidStack <spec>` form (Ulalek's main copy): it routes through the shared `knownDefinedTargets` ValidStack resolver ONLY when some comma token actually names a stack kind, so the no-usable-token degradation to Spell-only (`state.StackKindTokens`) never leaks in -- a spec this build cannot parse fails closed to no copy, never to "every spell on the stack". Still stand-ins in the spell-cast arm: the Effect-GRANTED spell-cast copy triggers (Rowan/A-Rowan Scholar of Sparks' emblem line, Mendicant Core, Guidelight's Max-speed line -- outside `findTriggerForAbility`'s printed-face scan, the known wider `Effect` gap). The sub-copy half is now REAL (abcopy3): `state.StackKindTokenOf` parses the `Ability` base (Activated+Triggered, never Spell; the corpus's one `Defined$ ValidStack Ability` line is Ulalek's -- 0 TargetType$ lines carry the base), `effects/context.go`'s `validStackTokens` reads its `otherAbility` qualifier (recovering the `YouCtrl` half of the `+`-compound state's own qualifier switch ignores), and the exclusion anchors on the new `Ctx.ResolvingObj` (rules set it at both ability-resolution ctx sites -- resolveTop's branches and the resume arm; a Source-anchored exclusion would exclude nothing, since for an ability resolution Source is the source PERMANENT, not the wrapper) -- and on that wrapper's whole printed-ability family (same Source permanent AND same Ability pointer, which StackCopy preserves): the moment a SECOND instance or copy of the trigger sits on the stack, an id-only exclusion regresses -- each copy asks the same {C}{C} question and a deterministic host answers it identically forever (measured livelock on the real ulalek-eldrazi deck, seed 1019, ~280 paid resolutions before the watcher aborts). Oracle text would copy other instances and let each copy's controller decline; the no-decline stand-in is recorded here. The ValidStack copy arm stays SINGLE-TARGET -- with several matching objects it copies only the first in stack-arena order (Ulalek's oracle says "copy ALL spells ... all other ... abilities"); the plural arm is the recorded stand-in. Pinned by `TestUlalekSubAbilityCopiesOtherTriggeredAbility`, `TestValidStackAbilityBaseAdmitsBothAbilityKindsYouCtrl`, `TestValidStackOtherAbilityExcludesTheResolvingWrapper`, `TestValidStackOtherAbilityExcludesTheSameAbilityFamily`, `TestStackKindTokenOfAbilityBase`. | `effects/trigger_referents.go` (`TriggerAbility`), `rules/trigger_referents.go` (`abilityCastStackObject`), `effects/context.go`, `effects/count.go` (`refTargets`), `effects/copy.go`, `effects/changex.go` (`effChangeX`; `changeXAbilityObject` fallback only), `rules/stack.go`, `rules/resolution.go`, `rules/cumulative.go` | none on the role path -- the role-absent fallback's topmost scan is corpus-unreachable |

| **The three combat/sacrifice restriction statics are enforced but only their whitelisted shapes (combatrestriction1)**: `stat:CantAttack` is enforced per (attacker, defender) pair (`rules/layers.go` `attackBlocked`, consulted by `askAttackers`' option filter, `validateAttackers`, and `mustAttackRequired`'s new `attackPairAvailable` gate — CR 508.1d's "if able": a required creature whose every pair a CantAttack blocks is NOT counted, so the KAttackers decision never wedges; an all-pairs-blocked table resolves the declare-attackers step silently with the empty declaration, the no-attacker path), `stat:CantSacrifice` at every sacrifice candidate choke point (the new `effects.Host` method `SacrificeBlocked`, rules-implemented scanning BOTH the Effect-registered restrictions and the face statics: effSacrifice's player-target eligible pool and object-target paths, effSacrificeAll, and the cast/activation/mana/ward/unless Sac-cost candidate walks — nonManaCastable, the announced-X bound, sacAsk, manaSacrifices, unlessPaymentCandidates, ward's Sac arm), and `stat:MustAttack` by mustAttackRequired's board-wide `activeStatics("MustAttack")` walk (which REPLACED the face-only walk — an AURA-carried requirement, Fealty to the Realm's `ValidCreature$ Creature.EnchantedBy`, now reaches the enchanted creature; a conditional static now skips only ITSELF rather than denying the whole creature, the one deliberate semantics change). The Effect route registers `CantAttack`/`CantSacrifice` (Call for Aid's `DB$ Effect | RememberObjects$ TargetedPlayer & RememberedCard`), with the remembered PLAYER captured explicitly into the new `state.ContinuousEffect.RememberedPlayers` (effectRemembered records objects only, so `Target$ Player.IsRemembered` had nothing to resolve) and resolved by `restrictionPlayerSpecMatches` (comma-list Target$, `!IsRemembered` compounds; a face static passes an empty set, fail closed). The ENFORCED shapes are whitelisted: a face CantAttack/CantSacrifice static whose parameters are not ⊆ {Mode, ValidCard, Target, Description, Secondary} (`effects.CantRestrictionParamsReadable`, shared with effEffect's registration gate so the two paths cannot disagree) is SKIPPED, not enforced blanket — the permissive direction for a restriction. **Measured populations (GNU /usr/bin/grep over .cards/cardsfolder at the corpus pin):** CantAttack 271 files, 80 with an enforced line, 191 only conditional (UnlessDefender$ 45 lines, Cost$ 31, IsPresent$ 25, PresentCompare$ 22, CheckSVar$ 17, SVarCompare$ 14, ValidSA$ 6, ActiveZones$/PresentZone$/EffectZone$/Condition$/DefenderNotNearestToYouInChosenDirection$/RememberingAttacker$ one-offs); MustAttack 166 files, 125 with a whitelisted line, 41 only conditional (the MustAttack$ defender-scoping family — "attacks a player other than you", Condition$ Threshold/Delirium, IsPresent$/PresentCompare$, ValidPlayer$ NotedDefender, AffectedZone$); CantSacrifice 15 files, 6 enforced (assault_suit, hithlain_rope, call_for_aid, jon_irenicus_shattered_one, stilt_man_towering_terror, slicer), 9 only conditional (ValidCause$ Spell,Activated/SpellAbility.OppCtrl + ForCost$ — the "can't sacrifice to cast spells"/"opponents' spells can't make you sacrifice" family). Also still unread: the `Target$ ...,Planeswalker.*` walker half (13 files — no walker-attack path; the comma-split makes the walker clause match nobody while the player clause carries the read). `make report` moved 26392 (78.4%) → 26580 (78.9%), +188 — exactly the measured ceiling of cards whose ONLY missing primitives were these three stats; 0 of the 188 are in any repo deck, so the heads and the ratchet are unmoved (verified). **`stat:MustBlock` (27 files) is still read NOWHERE** and was deliberately left out of this round. Pinned by `TestFealtyToTheRealmEnchantedMustAttackEachCombat`, `TestFealtyToTheRealmCantAttackScoping` (Vow of Lightning — Fealty's own CantAttack half is vacuous: its control static keeps the bearer under the monarch's control and askAttackers never offers a pair against the active player), `TestCallForAidStolenCreaturesCantSacrifice`, `TestCallForAidCantAttackRememberedPlayer`, `TestMustAttackIfAbleCantAttackBlocksRequirement` | `rules/layers.go` (`SacrificeBlocked`, `attackBlocked`, `cantRestrictionParamsReadable`→`effects.CantRestrictionParamsReadable`, `restrictionPlayerTargetMatches`, `restrictionPlayerSpecMatches`), `rules/combat.go` (`mustAttackRequired`, `attackPairAvailable`, askAttackers' filter + silent empty shape, `validateAttackers`), `rules/cast.go` (`nonManaCastable`, the announced-X Sac bound, `sacAsk`), `rules/mana_activation.go` (`manaSacrifices`), `rules/unless_payment.go` (`unlessPaymentCandidates`), `rules/ward.go` (the ward Sac arm), `effects/zone.go` (`effSacrifice`), `effects/sacrificeall.go`, `effects/misc.go` (`effEffect`'s case, `effectRememberedPlayers`), `effects/registry.go` (Host), `state/continuous.go` (`RememberedPlayers`), `rules/statics.go` (the registration) | M4 (the conditional CantAttack/CantSacrifice/MustAttack parameter grammar — UnlessDefender$/ValidCause$/ForCost$/MustAttack$-defender/Condition$/CheckSVar$ —; stat:MustBlock; the walker Target$ half) |

| **`EachDamage` is registered but the grammar has measured remainders (each1)**: the core is real (`effects/damage.go` `effEachDamage`) — the damager set resolves through `ToEachOther$` (parent targets ∪ the sub's own answered ask, deduped, each member deals to the OTHERS), `DefinedDamagers$` (`knownDefinedTargets` plus the `Valid <filter>` battlefield sweep plus the one `Referent.YouCtrl` qualifier the corpus writes), or the `DamageAll` `ValidCards$` sweep; recipients by precedence `EachToItself$` > `ToEachOther$` > `Defined$` > the SA's own `ValidTgts$` answer (the mvts1 `PickedTargets`, never falling through to the parent's targets when the sub's own ask found no eligible candidates); `NumDmg$` is PER DAMAGER (a per-damager Ctx anchors `CardPower`/`CardToughness` on the damager, keeping the resolution's SVar table so Nissa's Judgment's `NumDmg$ X` resolves); one simultaneous damage batch, one `damageRider` per damager built through `newDamageRider` (so lifelink, the deathtouch mark, protection and `DamageDone` triggers all read the DAMAGER). **What still fails closed** (the standing convention — no Note, no damage): damager specs naming predicates the filter does not know — `signature_slam`'s `Creature.modified+YouCtrl` (1 line) and the one-off `Creature.YouCtrl+powerGE4`; `alpha_brawl`'s `ControlledBy TargetedController` (pg2's `ControlledBy` recognises only the Triggered* refs); any recipient `ValidTgts$` whose eligible set is empty (tandem_takedown degrades to its creature/planeswalker half — `Battle` is an unknown type word, battles are unimplemented engine-wide); an unresolvable `NumDmg` expression degrades to 0 (0 corpus lines carry one). Also unread: `TargetUnique$` on the two carriers (band_together, tandem_takedown — both vacuous today, each sub asks exactly one target; a future multi-target ask would need a distinctness rule), the `ValidDescription$`/`DamageDesc$` display-only params (Sarkhan the Mad), and `RememberDamaged$`/`ReplaceDyingDefined$` on an EachDamage (0 corpus lines). The qualified-referent arm reads only `YouCtrl`; a richer qualifier grammar is not built | `effects/damage.go` (`effEachDamage`, `eachDamagerTargets`) | M4 (the unknown damager predicates; a distinctness rule for multi-target asks) |
| **Bestow's bestowed cast is real but the three exotic bestow costs stay withheld (bestow1)**: a card printed with `K:Bestow` (measured 43 corpus files) now offers its own "Cast … (bestowed)" option beside the plain creature cast, from the hand AND the command zone (a bestowed commander, kestia_the_cultivator's shape): the bestow cost is paid INSTEAD of the mana cost (`bestowCost`, the replicateCost fail-closed convention), the target ask and the resolution run the ONE synthesized Aura attach SA (`bestowedAttachSA` — `SP$ Attach | ValidTgts$ Creature | Object$ Self`, hand-built because the creature face has no SP of its own; never added to the face, which would make the PLAIN cast target-bearing), keyed on the pay-time `FlagBestowed` CastInfo provenance (the `"bestowed"` name was appended to events' `flagNames` wire table — without it FlagsString returns "" and the whole flag silently never rides), and the permanent enters attached through the ordinary Aura tail (`effAttach`'s events.Attach on the stack, `moveResolvedOffStack`'s attached entry, pinned by the Siona precedent). **The type switch (CR 702.114e) is fully filter-visible**: derived live state (`state.Object.BestowedAttached` = `AttachedTo != 0` + the printed `K:Bestow`), applied at the layer-4 type derivation (`bestowedTypeSwitch` — the derived list drops Creature and gains Aura while attached; creature SUBTYPES deliberately stay, inert on an Aura), the effects filter grammar (`hasType`/`hasTypeCtx`), the combat reads (canAttack/canBlock), the creature SBAs (toughness-0), the convoke/harmonize/delirium-power creature scans, protection's `"creatures"` quality and the Ferocious condition — so a bestowed-attached card is an Aura (can be enchanted-onto targets' statics' `Card.EnchantedBy` read, counts in `Aura.YouCtrl`, cannot attack/block/be convoke-tapped/die to the creature SBA) and reverts to a creature when the attachment ends. **The detach reading (CR 702.114b) is detach-and-stay**: bearer left the battlefield, bearer stopped being a creature, or bearer gained protection from the bestowed card's colours → ONE detach Attach (no IDs) and the card stays on the battlefield as a creature again, never the Aura graveyard arms (pinned: the Archon re-becomes a 4/4 white Archon). **Withheld, unoffered, never degraded** (the replicate convention): `nyxborn_hydra`'s `{X}` (the announced-X machinery would plausibly price it, but no bestowed-X cast is proven end to end), `detectives_phoenix`'s `CollectEvidence<6>`, and `hypnotic_siren`'s `5 U U:GainControl` (the third colon field is Forge metadata for its unregistered `stat:GainControl` static, not cost text) — bestowCost withholds on `Cost.Unknown` OR `Cost.X`. Measured: the 8 of 43 carriers still blocked are blocked on OTHER primitives (Cavern Lampad kw:Intimidate, Indebted Spirit kw:Afterlife, Mogis's Warhound stat:MustAttack, Noble Quarry's Lure, Nyxborn Unicorn kw:Mentor, Sightless Brawler's can't-attack-alone, Spirespine stat:MustBlock, Springheart Nantuko api:CopyPermanent); `make report` moved 25955 → 25990 (+35). **Left open**: a bestowed cast still fires "whenever you cast a CREATURE spell" triggers (`spellCastMatches` reads the printed face; the bestowed spell is an Aura spell per CR 702.114c) and would NOT fire "whenever you cast an Aura spell" ones — no corpus reader was measured for either, so the nuance is recorded rather than modelled. Pinned by `TestCelestialArchonBestowedCastAttachesAuraToBearer`, `TestCelestialArchonBearerLeavesBecomesCreatureAgain`, `TestCelestialArchonPlainCastUnchanged`, `TestEidolonOfCountlessBattlesBestowTransition`, `TestBestowExoticCostsWithheld` | `rules/bestow.go` (`bestowCost`, `bestowedAttachSA`, the kw:Bestow registration), `rules/cast.go` (cost-mode map, `modeFlags`, `targetAsk`, the convoke/harmonize gates), `rules/legal.go` (the two offer blocks), `rules/stack.go` (resolveTop's SA substitution), `rules/attach.go` (the bestowed detach branch), `rules/layers.go` (`bestowedTypeSwitch`), `rules/combat.go`, `rules/sba.go`, `rules/statics.go`, `rules/protection.go`, `effects/filter.go` (`hasType`), `state/object.go` (`FlagBestowed`, `BestowedAttached`), `events/event.go` (`flagNames`) | M4 (the three exotic costs; the creature-spell/Aura-spell trigger nuance if a reader ever lands) |
| **`DB$ AddPhase` (Forge's AddPhaseEffect) is implemented (ap1) -- the extra-phase primitive, extra combat/beginning/upkeep**: the grant is one `events.ExtraPhase` (+1, a new Kind appended after XChange) whose fold is `state.Game.ExtraPhases` (the ExtraTurn/ExtraTurnQueue precedent one level up), and the consumer is `rules/turn.go`'s `extraPhaseBoundary` at the ONE ordinary step-advance site (advanceStep's tail) -- no step renumbering. The turn is treated as the phase LIST the grant spliced: leaving AfterStep consumes the grant (-1) and enters Entry; leaving the extra phase's RangeEnd completes it (-2) and the walk resumes at FollowedBy$ or, absent, AfterStep+1 (Forge's default followedBy -- so an extra Beginning spliced after Main2 resumes at the END STEP, never back into Main1). Completion and splicing chain at one boundary (Obeka's N upkeeps, all spliced after the same EndCombat, walk N times; two Aurelia grants after the same EndCombat give two extra combats). Measured corpus: **56 SA lines across 56 files** (`\b(AB|DB)\$ AddPhase\b`); `ExtraPhase$` values Combat 48 / Beginning 4 / Upkeep 3 / End of Turn 1; `AfterPhase$` EndCombat 34, "End of Turn" 1, omitted 21 (the omitted form splices after the phase the grant resolved in -- the granting step rides the event so replay folds the same splice); `FollowedBy$ Main2` 12; `SubAbility$` 8 (free, the ordinary walk). `ConditionFirstCombat$` (Raiyuu, A-Raiyuu, Tifa -- 3 lines) is READ via the folded per-turn combat count `state.Game.CombatsThisTurn` (one increment per BeginCombat entry, reset at TurnChange) -- without it the gate failed OPEN and Raiyuu's lone-attack trigger re-granted an extra combat from every combat the first one created (pinned: `TestRaiyuuFirstCombatGateBlocksTheSecondGrant`). `NumPhases$` (Obeka's `TriggerCount$DamageAmount`, 1 line) resolves through the ordinary Num evaluator. The `ExtraPhaseDelayedTrigger$/ExtraPhaseDelayedTriggerExcute$` pair (3 lines, Moraug; the corpus "Excute" typo read verbatim) forwards the delayed trigger on the grant via `delayedTriggerSpec`, which reads BOTH definition-body shapes -- the raw `Mode$ Phase |...` body (parseSA cannot read it: no DB$ head, so the old ResolveSVar path NEVER fired) and the keyword expansions' `DB$ DelayedTrigger |...` body -- registering at CONSUME time with MinTurn = the current turn (the same fix taught `effAddTurn` the raw shape, so Alchemist's Gambit's Upkeep registration stops degrading to the end step; the only behaviour change outside AddPhase, no repo deck carries it). `make report` moved 26143 (77.7%) -> 26178 (77.8%) (+35 -- the 35 cards blocked on api:AddPhase and nothing else; no carrier is in any repo deck, heads verified unmoved). **Loud-degrades** (one Note naming the param, grant still applied where a grant is possible): unresolvable `ExtraPhase$`/`AfterPhase$`/`FollowedBy$`/`NumPhases$`/DelTrig values, multi-step AfterPhase$/FollowedBy$ sets. **Deliberate narrowings**: a grant whose splice point this turn has already passed is dropped silently at TurnChange (no corpus carrier -- AfterPhase$ EndCombat carriers always resolve during combat, and the omitted-AfterPhase Combat carriers all gate on `ConditionPhases$ Main1,Main2`); a splice at a MID-RANGE step of another active extra phase is handled by the same rules (the consumed entries persist until their own RangeEnd) but is corpus-unreachable and untested by any corpus shape; the multi-completion resume choice at one boundary takes the LAST completed entry's resume point (likewise corpus-unreachable). Pinned by `TestAureliaExtraCombatSplicesASecondCombat`, `TestMoraugExtraCombatUntapsAtItsBeginning`, `TestEomerExtraCombatChainsThroughTheTrigger`, `TestAggravatedAssaultExtraCombatFollowedByMain2`, `TestExtraPhaseQueueDoesNotSurviveTheTurn`, `events/extra_phase_test.go`'s fold pins; every engine test replay-verifies byte-identically | `effects/addphase.go` (`effAddPhase`, `delayedTriggerSpec`, `parseExtraPhaseValue`), `state/game.go` (`ExtraPhase`, `ExtraPhases`, `CombatsThisTurn`), `state/phase.go` (`ExtraPhaseRangeEnd`), `events/event.go` (ExtraPhase Kind, `ExtraPhaseRiders`), `events/apply.go` (the ExtraPhase case, the StepChange/TurnChange counters), `rules/turn.go` (`extraPhaseBoundary`, `consumedExtraPhaseEvent`), `effects/conditions.go` (`ConditionFirstCombat`) | M4 (a real per-turn attack history so the unread `FirstAttack$` gate -- 4 corpus carriers, Aurelia's "for the first time each turn" -- can stop the trigger-side cascade the extra combats expose) |
| **A `Mode$ Continuous` static's `Goad$ True` rider is read on the static's own face, but three grant-delivered statics stay un-goaded (staticgoad1)**: `rules/combat.go`'s `staticGoaders` feeds both CR 701.38b detectors (`hasActiveGoad` for the "attacks if able" requirement, `goadedBy` for the "a player other than each goader" restriction) from every live `activeStatics("Continuous")` whose `Goad$ True` and whose `Affected$` (default `Card.Self`) matches the creature, the goader being `sv.Controller` — so Shiny Impetus/Bloodthirsty Blade/the Impetus cycle and Baeloth Barrityl (whose `Affected$ Creature.powerLTY+OppCtrl` resolves through `specCtx`'s SVar resolver) goad for real, and the goad ends with the source because the static is re-derived per call. What is NOT read is the same static delivered by `AddStaticAbilities$` (`DB$ Clone` — Mocking Doppelganger) or `StaticAbilities$` (`DB$ Effect` — Hot Pursuit, Immortal Obligation): `activeStatics` walks printed `Face.Statics` only and does not expand those grants, so those 3 cards' `Goad$ True` statics are invisible. Measured at the corpus pin: 17 raw `.cards/cardsfolder` files carry `Goad$ True`, 14 read directly (12 Auras, Bloodthirsty Blade's `Creature.EquippedBy`, Baeloth's printed creature static), 3 grant-delivered. Separately, a statically goaded creature is invisible to the `Creature.IsGoaded` filter predicate, which reads only the event-backed `state.Object.Goads` list (open issue `agent-20260919T203859Z-269892c3`). No repo deck carries any of the 17 cards, so `TestHeads` is unmoved (verified). Pinned by `TestStaticGoadShinyImpetusForcesAttackAway`, `TestStaticGoadEndsWhenAuraLeaves`, `TestStaticGoadBaelothPowerThreshold` | `rules/combat.go` (`staticGoaders`, `hasActiveGoad`, `goadedBy`) | M4 (expanding `AddStaticAbility$`/`StaticAbilities$` grants in `activeStatics`; the `IsGoaded` predicate reading the static route) |
| **`Cost$ Mandatory PayLife<X>` bodies (3 corpus cards) still run their body for free and never pay, and their body API is unregistered**: minion_of_the_wastes, nameless_race and phyrexian_processor each carry a *replacement* body (`R:Event$ Moved | ReplaceWith$ PayLife`) whose `SVar:PayLife:AB$ StoreSVar | Cost$ Mandatory PayLife<X>` is a mandatory life payment gated on a resolved amount. Two independent blockers keep this out of the mandatory settle trigmand1 added for the analogous `Mandatory Sac`/`Exile` trigger bodies (rules/cumulative.go `triggerBodyNeedsCostWindow` -> `mandatorySettleShape`, which arms only the Sac/Exile shapes and deliberately leaves this family on today's free execution): (1) `DB$ StoreSVar` is not registered as an effect at all (`/usr/bin/grep -rn '"StoreSVar"' effects/` returns nothing), so the replacement body already hits the generic "unimplemented API" fallback and moves nothing BEFORE cost payment is considered; (2) the body is a replacement (`runReplaceWith` -> `resolveReplacementWith`), not a trigger body, so `triggerBodyNeedsCostWindow` is not on its path, and `PayLife<X>`'s X is a resolved amount the body reads back through `Count$xPaid`, which needs an X-announcement ask and an `xPaid` binding the trigger window has no channel for. Measured at the corpus pin with GNU `/usr/bin/grep` over `.cards/cardsfolder`: `Cost$ Mandatory PayLife` = **3** lines / **3** files; `ReplaceWith$ PayLife` = **3** files; `Cost$ Mandatory Sac` = 15 lines (14 ordinary trigger bodies settled by trigmand1 + ominous_lockbox's `AB$ CopySpellAbility` carve-out, already an abcopy1 decline-only stand-in) and `Cost$ Mandatory Exile` = 1 (dalek_intensive_care). The `Cost$ Mandatory PayEnergy` "8 lines" a loose substring grep reports are all really `UnlessCost$ Mandatory PayEnergy` (measured: `/usr/bin/grep -rE 'UnlessCost\$ Mandatory' .cards/cardsfolder | wc -l` = 9 = 8 PayEnergy + 1 ExileFromGrave), which already route through the unless-pay gate; and `ominous_lockbox` (the `API == "CopySpellAbility"` carve-out one line above in `triggerBodyNeedsCostWindow`) is handled/stand-in by the ability-cast copy window. Separately, `Dalek Intensive Care`'s own command-zone triggers can never fire in this build: `forEachObject` (rules/trigger_match.go) walks zones `ZLibrary..ZStack` only and never the appended `ZCommand`, and `PlaneswalkedTo` is not a registered trigger mode -- so trigmand1 tests the card's exact `Exile<1/Creature.nonDalek/non-Dalek creature>` cost shape on a fixture carrying the real spec rather than firing the real card. | `rules/cumulative.go` (`mandatorySettleShape`, `advanceTriggeredMandatory`, `settleTriggeredMandatory`), `rules/replacement.go` (`runReplaceWith`, `resolveReplacementWith`), `rules/mana.go` (`Cost.LifeX`), `rules/x_paid_test.go` (`Count$xPaid`) | M4 (register `StoreSVar`; an X-announcement + `xPaid` binding for a replacement body; command-zone trigger scanning and a `PlaneswalkedTo` trigger mode) |
| **`TriggersWhenSpent$` (the mana-spent rider) is real but only the `Mode$ SpellCast` shape of its SVar runs (mtsp1)**: a mana ability's `TriggersWhenSpent$ <SVar>` param makes `effMana` emit its adds as an UNRESTRICTED provenance batch (`events.ManaRestrictionText("", c.Source)`, `Valid == ""` so the mana is spendable anywhere), `emitRestrictedManaSpend` captures each consumed batch's `Source` into `Engine.manaSpentSources` during a SPELL payment, and `payCast`'s `fireManaSpentTriggers` queues the named SVar's trigger after `fireDeferredCastTrigger` (gated by `zoneGate`/`phaseGate`/`spellCastEval`, queued after the ordinary cast triggers). Measured corpus: **13 raw files** carry the param, all `A:AB$ Mana`; **0 of 13** pair it with `RestrictValid$`/`AddsNoCounter$`, so the restriction encoding always wins where one exists (one loud Note records the dropped provenance) and no live overlap exists. **Remainders:** `Mode$ SpellAbilityCast` (sunken_palace's "spell or activate an ability") is deliberately unsupported — only `Mode$ SpellCast` fires (1 carrier, not in any repo deck); ability activations, the unless-pay arm and every non-spell payment fire nothing (the rider is a cast-spend gate); and the other carriers' EXECUTE bodies hit known engine limits unrelated to the rider — `DB$ CopySpellAbility | MayChooseTarget$ True` (pyromancers_goggles, primal_amulet, sunken_palace) keeps its target per the copy stand-in, the `DB$ Effect` bodies (jade_orb, forgers_foundry) register only CantTarget/CantRegenerate, and andrios_roaming_explorer needs `DB$ Animate | Duration$ Permanent` on a stack spell. Path of Ancestry is the pin (`TestPathOfAncestryScryOnMatchingCast` + the two spend-gate negatives). The `Commander` referent (`sharesCreatureTypeWith Commander`, the corpus's only carrier) now resolves through `sharesTypeReferents` to the source's controller's commanders. `make report` unmoved (27089 → 27089, verified against the parent commit): no primitive was registered. Heads unmoved (0 of 13 carriers in any legacy golden deck). | `effects/misc.go` (`effMana`), `rules/stack.go` (`emitRestrictedManaSpend`), `rules/cast.go` (`fireManaSpentTriggers`, `svarBodyForObject`), `rules/engine.go` (`manaSpentSources`), `effects/filter.go` (`sharesTypeArg`, `sharesTypeReferents`) | M4 (ability-activation and `SpellAbilityCast` riders; the copy/Effect/Animate execute bodies) |
| `Count$CastTotalManaSpent` (tasks castfilter1/castfilter2): the BARE head is the pay-time FlagManaSpent capture (castprov1) and is untouched. The FILTERED form `Count$CastTotalManaSpent <Type>` counts only mana spent whose SOURCE was a permanent of `<Type>`: `Snow` resolves from the parallel snow tally the pool has always carried (`state.Player.Snow`, CR 107.4h), and `Treasure`/`Cave`/`Desert` resolve from the per-unit producer tally the pool now carries (`state.Player.TypedMana [3]Mana`, the `"TreasureC"`/`"CaveW"`/`"DesertR"` ManaAdd Counter form beside the historical `"S<colour>"` — effMana tags a Treasure/Cave/Desert producer's units from its Face().Types, fixed Treasure > Cave > Desert precedence, measured: no corpus producer carries two, and none is both Snow and typed). The payment consumes a PLAIN unit before a typed one and a typed one before snow (snow stays reserved for {S} pips; typed units have no pips of their own), each tag's spend delta is captured at `payCast` into `pc.manaSpentTreasure/Cave/Desert` and rides its OWN trailing pay-time CastInfo (`FlagManaTreasureSpent`/`FlagManaCaveSpent`/`FlagManaDesertSpent`, folded into `state.Object.ManaTreasureSpent`/`ManaCaveSpent`/`ManaDesertSpent`; each later event carries all earlier flags, so the Apply switch checks the NEWEST flag first — Desert, Cave, Treasure, Snow, then the total — or every later event would route into the first tag's field). Measured corpus (GNU `/usr/bin/grep` at `FORGE_REF 95f04e8a`): **32 files carry the head, 15 filtered** — Snow 6 (Berg Strider, Blood on the Snow, Blessing of Frost, Tundra Fumarole, Graven Lore, Search for Glory), Treasure 7 (Marut, Devour Intellect, Hired Hexblade, Price of Loyalty, Pirate's Landing, Jaded Sell-Sword, Spoils of the Hunt), Desert 1 (Cataclysmic Prospecting), Cave 1 (Bat Colony). Deliberate narrowings: the two OTHER positive ManaAdd producers (cumulative-upkeep AddMana, ManaReflected) stay UNTAGGED. A restricted-batch overlap with typed units IS handled: a Treasure/Cave/Desert producer that also carries `RestrictValid$`/`AddsNoCounter$` (measured 3 real carriers — `echoing_cavern` and `sunken_citadel`, both Land Cave, and `bucolic_ranch`, Land Desert) registers its restriction batch exactly like a plain one, and the payment's `manaAvailableFor` hides an unusable batch's typed provenance in the typed tally as well as the pool — `takeUnit` partitions a slot by that tally, so a visible typed unit under an unusable restriction could otherwise be consumed through the typed path. The restricted carve consumes a batch only up to the units the payment search actually attributed to it (a tagged batch only the tag's spent units, a plain batch only the slot's remaining plain units), so a restricted tagged unit is emitted once, never twice, and a batch the plain-first search never touched is left intact rather than driving the emission split's tally negative and over-spending the pool. The 3 carriers are pinned on the real cards/compiled shapes below. Unknown `<Type>` args keep the fail-closed 0. Pinned by `TestCastTotalManaSpentHeadFiltersByType`, `TestCastTotalManaSpentHeadReadsTypedFields` + `TestCastTotalManaSpentTypedReadTheRealCorpusSVars` (effects, real Marut/Bat Colony/Cataclysmic SVars), `TestTypedManaTallyMovesWithThePool` + `TestCastInfoTypedFlagsRouteNewestFirst` (events) and the end-to-end rules leaves `TestCastTotalManaSpentSnowCountsOnlySnowEndToEnd`, `TestCataclysmicProspectingCastTotalManaSpentFailsClosed` (plain mana creates 0), `TestCastTotalManaSpentTreasureMarutCreatesTreasureTokens` / `TestCastTotalManaSpentPlainOnlyCreatesNoTokens` / `TestCastTotalManaSpentTreasureExactnessOneOfEight` / `TestCastTotalManaSpentMixedPoolCaptureSplitsEveryTag`, `TestCastTotalManaSpentDesertCataclysmicProspectingCreatesTappedTreasures`, `TestCastTotalManaSpentCaveBatColonyCreatesBatTokens`, `TestCastTotalManaSpentConsumesPlainBeforeTyped`, the restriction-carrying-tag pins `TestBucolicRanchRestrictedDesertManaKeepsItsRestriction`, `TestRestrictedTaggedManaSpendIsNotDoubleCounted`, `TestRestrictedTaggedManaSpendNeverOverSpends` (a pool larger than the cost), `TestRestrictedTaggedManaPartialConsumptionIsExact` and `TestEmitManaTagsTreasureProducerType` (the producer-side tagging). None of the 15 filtered carriers is in any repo deck, so `TestHeads` is unmoved (verified). | `effects/count.go` (`evalCountBody`'s `CastTotalManaSpent` case), `effects/misc.go` (`effMana`'s tagging), `rules/mana.go` (`takeUnit`'s plain→typed→snow order, `resolveManaWith`'s typed param), `rules/stack.go` (`payManaForSpent`'s `spentSnow`/`spentTyped` returns, `manaAvailableFor`'s typed filtering, `emitRestrictedManaSpend`'s carve, `payManaCastSpent`), `state/ids.go` (`ManaSlot`/`TypedManaCounter`), `rules/cast.go` (`pc.manaSpentSnow` + the three typed captures, `payCast`'s trailing CastInfos), `state/game.go` (`Player.TypedMana`), `state/object.go` (the three flags + fields), `events/event.go` (`flagNames` rows), `events/apply.go` (the typed Counter parse, CastInfo fold + the two resets) | M4 remains for the `TriggeredCard$CastTotalManaSpent` ref-head (the rv2b exotic-heads row) and the untagged producers above; `AIPreference:ManaFrom$` bot hints are not engine policy |

## Trigger-relative filter arguments (pg2)

`ControlledBy <ref>` and `OwnedBy <ref>` recognise exactly `TriggeredTarget`,
`TriggeredDefendingPlayer`, `TriggeredPlayer`, and `TriggeredCard`. The matcher
and `UnknownPredicates` share one classifier. An absent binding fails closed,
including under `!`; player references carry an explicit presence bit so seat
zero is not mistaken for absence. `Targeted*` and `Spawner>...` chains remain
unknown. This is not a general Defined$/player-selector implementation.

`effects.TriggerContext`, embedded in `Ctx` and `SpecContext`, keeps event roles
separate from the resolving ability's source, chosen targets and Remembered.
Rules captures damage recipients/sources, BecomesTarget's targeted object and
causing source, attack defenders, phase players, and zone-change/cast cards.
Per-stack-instance bindings survive target placement, rechecks, suspension,
cloning and stack copying, and are rebuilt by engine replay without new events
or event fields. A multi-attacker event retains its defender but leaves the
singular card/source absent if multiple attackers matched. Unsupported trigger
modes and delayed registrations without these roles do not invent bindings.

**Limits:** `CombatDamage$ True`/`False` triggers ARE enabled (fb-20260914T145022Z-64a8422c:
`damageMatches` reads the engine-side `combatDamaging` flag `dealCombatDamage` sets around
each assignment; no event-schema change), but this does not implement `TargetingPlayer$`
(Magus of the Abyss still asks the
trigger controller, not necessarily the upkeep player), carry these bindings
into a registered continuous effect, or add LKI ownership/control snapshots for
a referent that changes before resolution. Object referents read their live
owner/controller. Existing Self/Other target-source semantics are unchanged;
normalising them is separate work (notably Flickerwisp's `Permanent.Other`).
Master of Diversion's defender-scoped Tap is pinned end to end on its compiled
corpus SA in `rules/trigger_referents_test.go`; the target/source discrimination
and suspension probe in that file is deliberately synthetic.

## Regeneration behaviour (implemented, not an approximation)

Stated here rather than in the approximations table above, because it is
implemented rather than outstanding: its old row carried a `Removed by` of
`M4 (Effect continuous restrictions)` for restrictions that already work.

Lethal damage that would destroy a permanent, and `Destroy`/`DestroyAll`, are
replaced by the destruction-replacement path when the permanent has an unused
this-turn Shield: the replacement consumes the shield, clears all damage,
taps the permanent and removes it from combat; unused shields expire at
cleanup. Destroy-side `NoRegen$` is modelled in compiled SA parameters and
honoured by both destruction APIs, and the `Effect`-registered
`Mode$ CantRegenerate` restriction (Incinerate's "can't be regenerated this
turn") is honoured through `Host.RegenerationDisallowed`, so no
continuous-restriction static remains unmodelled (see the `Effect` row for
the wider `Effect` duration work). Pinned by `TestRegenerationCombat`, `TestRegenerationDestroy`,
`TestRegenerationCannotReplaceNoRegen`, `TestRegenerationExpires`,
`TestRegenerationExperimentOne` and `TestIncinerateCantRegenerate`. The audit
here is deliberately narrow: this certifies only those named restriction
leaves, not every `Effect` duration. Implementation lives in
`effects/regeneration.go`, `effects/zone.go`, `rules/sba.go` and
`rules/combat.go` (the old row's `Where` column).

## The board clock's round number (not an approximation)

Stated here rather than in the approximations table above: the round number
is EXACT, so it is not a stand-in and no milestone owes its removal. It sat
in that table as a malformed one-cell row -- no `Where`, no `Removed by` --
which is the one shape that table forbids, since a deferral is only as good
as the milestone still owing it.

The board clock's **round number is now the EXACT round-trip count, folded over the ordered event stream** (`view.RoundOf`), not a projection of the current snapshot. host computes it where the log is in hand -- the fan-out snapshot the client's board clock reads (`host/fanout.go`), the DVR's `viewAt` at an arbitrary seq (`host/viewat.go`), and the seat-decision view (`host/match.go`); `cmd/mtgsim`, `cmd/botbench` and `cmd/keywordbench` do the same. The rule, stated once: the fold is ANCHORED ON THE GAME'S STARTING PLAYER -- the seat the first TurnChange hands the turn to (the CR 103.1 toss winner resolved over the survivors); turn order is the cyclic order beginning there, NOT at seat 0 (rv2a: before the toss the two coincided, and a fold still anchored at seat 0 ran one round ahead all game for every game whose toss started another seat -- ~half of 2-seat and ~3/4 of 4-seat games). A round boundary is a TurnChange that hands the turn to the FIRST STILL-LIVING SEAT in that cyclic order, round 1 is the initial pass, and the first TurnChange never increments (it establishes the anchor). The fold tracks the alive set itself by folding `PlayerLost` events, so it knows the first survivor at every moment -- the thing a snapshot cannot recover -- and this is what closes the jump: the old projection re-divided by the now-smaller alive set the moment a seat died (11 -> 14 -> 21 on the brief's measured table), while the exact count advances only when play returns to the anchored seat, so an elimination on its own changes nothing -- the displayed round stays put until the next genuine turn-over, and then rises by one (a seat at a time), never by a projection artifact. Before the first elimination the fold and the old `1 + (Turn-1)/AliveCount` agree exactly WHEN the starter is seat 0 (the snapshot cannot recover the starter); they diverge after a death or a non-zero starter, where the fold is right and the projection runs ahead. The edge cases the rule is written to: a seat eliminated mid-round adds no boundary (the survivors just get a shorter cycle); a seat eliminated who was the anchor (the starting player) moves the anchor to the next living seat in turn order from the starter, and the next turn to reach that new anchor opens a round -- the case that defeats a naive "active index fell" rule, since the turn can rise an index yet still be a round boundary; and the round before any turn is 1. The number is identical on a live game and a log-only reconstruction (`replay.Replay` reproduces the same events byte for byte, and the fold is deterministic), pinned by `TestRoundFoldsIdenticallyAcrossReplay` and `TestRoundOfFoldsEliminationWithoutJump` plus rv2a's anchored pins (`TestRoundOfAnchorsOnTheStartingSeat`, `TestRoundOfStarterEliminatedMovesTheAnchor`).

`roundOf` (the old snapshot projection) is KEPT as the fallback for a caller that genuinely has only a `state.Game` (this package's own tests and the absence of a log); its doc says it is the approximation and names the exact one. The fold is a projection and never touches engine state: it writes no event, cannot change a chain head, and `g.Turn` is untouched -- the transcript still records the true per-player "Turn N: <player>" via `view/describe.go`. What still does not complicate this: no `AddTurn` primitive is registered, so the 65 corpus files carrying extra-turn script route to the `unimplemented` fallback and no seat can take two turns in one pass (the fold's doc notes it must be revisited if a same-seat repeat turn ever lands). | `view/view.go` (`RoundOf`, `roundOf`), `host/fanout.go`, `host/viewat.go`, `host/match.go`, rendered by `web/src/components/PhaseTrack.svelte` | closed: the exact fold replaced the snapshot projection for every log-bearing caller |

## Host behaviour notes (embedder observer hooks, D15)

`OnBurst` errors crash the match like a persist failure (D15): the table
halts and the chain does not continue. `OnMatchEnd` errors are discarded
because the outcome is already recorded and an error cannot un-record it, so
an embedder that persists through `OnMatchEnd` must handle its own
persistence failures inside the callback.

## Seat-privacy boundary

A seat credential names **both its table and seat**. Every current
seat-authorised HTTP route (`view`, `events`, `pending`, `intent`, `undo`)
goes through `host/httpapi`'s `claimForTable`; a mismatched or legacy unbound
claim is 403 and must never reach registry state. `cmd/gorged` mints startup
and vs-bot tokens with that pair, so after deployment old tokens stop working;
the web client returns a rejected stale join to the lobby, where it obtains a
fresh game claim. Stream session ids are random 128-bit values issued only in
`hello`; `host.Session.serial`, not the public id, preserves fan-out order.

## Running a gorged server while you work

Two orchestrator sessions share this box, and one of them serves a live demo
that is redeployed automatically after every merge into `main`. So ports are
allocated, not first-come:

| range | who |
|---|---|
| 8080-8081 | the demo. **Never bind these**, and never run `make deploy-demo` |
| 8082-8089 | the bot-policy / botbench workstream |
| 8090-8099 | task agents on the engine side — pick one of these |

Put persistence under `/tmp/gorge-<something-unique>`, never the repo-root
default `gorged-data`: several servers sharing one persistence directory
silently corrupt each other, and a resumed directory written by an older binary
comes back with the fields that binary lacked set to their zero values
(`Format`'s zero is `constructed`, a real value, so `-format` is ignored with
nothing in the output to say so).

**Stopping a server: never `pkill -f` or a bare `pgrep -f`.** The pattern is
matched against every process's `/proc/<pid>/cmdline` including your own shell's,
and it has killed a session here. Find the server by its listening socket
(`ss -lptn`), confirm the pid, then signal that pid. Note that `/proc/<pid>/comm`
is the BINARY's name -- if you built `gorged-after`, its `comm` is
`gorged-after`, not `gorged` -- so match on the port rather than the name.

`scripts/fleet.sh ports` prints the current allocation, and `scripts/fleet.sh
port` prints a free one in your range.

## Working in a task worktree

Task worktrees are created with `scripts/agent-worktree.sh <id> [base] [--web]`,
never with a bare `git worktree add`. A worktree carries only tracked files, and
this repo needs one untracked thing to test honestly: the `.cards` corpus. Without
it `internal/testutil`'s `CorpusRegistry` calls `t.Skip`, so every
corpus-dependent test SKIPS instead of running, the package still prints `ok` --
in about 2ms -- and the run reads green to you, to the review pre-filter and to
the gate while having executed almost nothing. If you find yourself in a worktree
with no `.cards`, stop and say so rather than reporting a green suite.

Which agent seats exist, what they cost and when to escalate between them is
recorded in `docs/superpowers/agent-seats.md`.
