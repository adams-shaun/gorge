# agentctl — moved

The agentctl design and implementation plan now live in their own repo:
`~/projects/agentctl` (`docs/design.md`, `docs/plan.md`).

gorge is a consumer: see design §13 (adoption tiers, `.agentctl/config.toml`)
and plan slices 0, 1.5, 2.J2, 5, 7 for the gorge-side changes.
